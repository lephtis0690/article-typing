const TEXTS_FALLBACK = [
  {
    "id": "digital-democracy",
    "title": "デジタル社会の進展と民主主義の行方",
    "text": "デジタル社会の進展と民主主義の行方\n　二十一世紀に入り四半世紀が過ぎた今、私たちはかつて想像もできなかった速度で情報が飛び交う時代を生きている。インターネットは世界を結び、スマートフォンはあらゆる知識へのアクセスを手のひらに収めた。だが同時に、この便利さの裏側に深刻な影が広がっていることを、私たちは直視しなければならない。\n　まず問題として浮かび上がるのが、情報の信頼性をめぐる混乱である。かつて情報の発信者は限られていた。新聞社や放送局といった組織が取材を重ね、編集のふるいをかけた上で、読者や視聴者のもとへ届けていた。その仕組みには批判もあり、権力との癒着や報道の偏りが指摘される場面も少なくなかった。しかし少なくとも、情報を選別する何らかのプロセスは存在していた。\n　ところが今日、誰もが発信者となれる環境が生まれたことで、正確な情報と根拠のない臆測とが同じ土俵に並ぶようになった。アルゴリズムは真偽よりもエンゲージメントを優先し、感情を刺激するコンテンツほど拡散されやすい。人々は自分の信念を強化する情報ばかりに囲まれ、異なる意見や事実と向き合う機会を失いつつある。この「フィルターバブル」の問題は、民主主義の根幹を揺るがしかねない。\n　民主主義とは本来、多様な意見を持つ市民が対話を重ね、合意を形成していく営みである。そのためには、共通の事実認識が不可欠だ。同じ出来事について、まったく異なる「現実」を信じる人々が対立する社会では、建設的な議論など成り立ちようがない。近年、各国で顕著になっている政治的分極化の背後には、こうした情報環境の変容が大きく関わっていると考えられる。\n　次に取り上げたいのが、人工知能の急速な普及がもたらす課題である。生成AIと呼ばれる技術は、文章や画像を驚くほど自然な形で作り出すことができる。これは創造的な仕事の可能性を広げる一方、フェイク動画や偽情報の生産コストを劇的に下げる。専門家でさえ、本物と偽物の見分けが難しくなる日は遠くないかもしれない。\n　また、AIの導入は労働市場にも深刻な影響を及ぼしつつある。繰り返し作業や定型業務にとどまらず、専門知識を要するとされてきた職種においても、AIが人間の業務を代替し始めている。この変化は生産性の向上をもたらす面もあるが、同時に雇用の喪失という現実的な苦しみを多くの人に与えている。社会のセーフティネットや教育制度が、変化の速度に追いついていないという懸念は根強い。\n　さらに、デジタル化が進む中でプライバシーの問題も深刻化している。私たちが日々スマートフォンやウェブサービスを使うたびに、膨大な個人データが生成・収集されている。企業はそのデータを分析して行動を予測し、マーケティングや商品開発に活用する。それ自体は必ずしも悪いことではないかもしれないが、データがどのように扱われているかについて、利用者が十分な情報を持ち、真の意味で同意しているとは言い難い現状がある。\n　国家による監視の問題も見過ごせない。テクノロジーを駆使した監視システムは、治安維持や犯罪捜査に効果を上げる場面がある一方、権威主義的な政権のもとでは市民の自由や反体制的な活動を抑圧する道具ともなり得る。民主主義国家においても、安全保障を名目とした監視の拡大が個人の自由と緊張関係を生んでいる。その均衡をどこに求めるかは、社会全体で議論しなければならない問いである。\n　こうした課題に対して、私たちは何をなすべきか。まず求められるのは、メディアリテラシーの教育を早期から体系的に行うことである。情報を批判的に読み解く力、一次資料にあたる習慣、異なる立場の意見に触れる姿勢を、学校教育の段階から養う必要がある。デジタル技術の使い方を教えるだけでなく、デジタル空間において考える力を育てることが、これからの教育に求められている。\n　同時に、プラットフォーム企業に対する適切な規制の枠組みも整備しなければならない。巨大テクノロジー企業が持つ情報流通への影響力は、もはや一私企業の問題にとどまらない。公共財ともいえるインターネット上の言論空間をどのように守るか、各国政府や国際機関が真剣に取り組む時期に来ている。ただし、規制は表現の自由を損なわない形で慎重に設計されなければならない。\n　AIについては、その開発と活用に倫理的な基準を設けることが急務である。誰のためのAIか、どのような価値観を反映させるべきか、そして意図しない被害が生じた場合の責任は誰が負うのか。これらの問いに答えを出さないまま技術だけが先行することは危険である。研究者、企業、政府、そして市民社会が協力して、持続可能なAIガバナンスの枠組みを構築する必要がある。\n　労働市場の変容に対しては、継続的な学び直しを支援する制度の拡充が欠かせない。技術の変化に対応できるよう、成人が何度でも教育を受け直せる環境を整えること、そしてAIによる生産性向上の恩恵が社会全体に公平に配分される仕組みを考えることが求められる。\n　翻って考えれば、デジタル技術そのものは中立である。それが民主主義を支える力になるか、あるいは脅かす力になるかは、私たち人間の選択にかかっている。利便性に流されるままに技術を受け入れるのではなく、一人ひとりが情報社会の構造を理解し、より良い社会の実現に向けて能動的に関わっていく姿勢こそが、この時代に求められている。\n　歴史を振り返れば、印刷技術の発明が宗教改革をもたらし、電信や電話が国際政治の様相を塗り替えた。テクノロジーの革新は常に社会の深部に変容を迫ってきた。今私たちが経験しているデジタル革命も、その流れの延長線上にある。だからこそ、今この瞬間に、社会のあり方について真剣に問い直すことが求められているのだ。未来を形づくるのは、技術そのものではなく、それを使いこなす私たち自身である。"
  },
  {
    "id": "science-technology-future",
    "title": "科学技術の発展と未来社会",
    "text": "　近年、科学技術の発展は私たちの生活を大きく変え続けている。かつては専門家だけが扱うものだと思われていたコンピュータや通信技術も、今ではスマートフォンという形で誰もが日常的に利用する時代となった。朝起きて天気予報を確認し、電車の運行状況を調べ、学校や職場へ連絡を送る。そのすべてに情報通信技術が関わっている。科学技術は特別な研究施設の中だけに存在するものではなく、私たちの日常そのものを支える基盤となっているのである。\n　その中でも特に注目されているのが、人工知能、いわゆるＡＩの発展である。ＡＩは大量のデータを分析し、人間では見つけにくい特徴や規則性を発見することができる。近年では画像認識や音声認識の精度が大きく向上し、スマートフォンの顔認証や、自動翻訳機能などにも利用されるようになった。以前は機械による翻訳は不自然な文章になることが多かったが、現在ではかなり自然な表現で翻訳を行えるようになっている。外国語を学ぶ際にもＡＩを活用した学習支援サービスが増えており、生徒一人ひとりに合わせた学習内容を提示できるようになりつつある。\n　また、医療分野においても科学技術の進歩は大きな役割を果たしている。例えば、画像診断支援ＡＩは、レントゲン写真やＣＴ画像を解析し、病気の兆候を発見する補助を行っている。もちろん最終的な判断は医師が行う必要があるが、ＡＩが補助をすることで見落としを減らし、診断の精度向上につながることが期待されている。さらに、遠隔医療の技術も発展しており、離れた場所に住んでいる患者がオンラインで診察を受けることも可能になってきた。特に高齢化が進む地域では、こうした技術の重要性が高まっている。\n　さらに、自動運転技術についても注目が集まっている。自動運転車は、カメラやセンサーを用いて周囲の状況を把握し、コンピュータが運転操作を行う仕組みである。もし実用化がさらに進めば、高齢者の移動支援や交通事故の減少につながる可能性がある。日本では高齢化が進んでおり、地方では公共交通機関の減少が課題となっている地域も少なくない。そのため、自動運転技術によって移動手段を確保できれば、多くの人々の生活を支えることにつながるだろう。\n　エネルギー分野でも科学技術は重要な役割を果たしている。現在、世界では地球温暖化への対策が求められており、二酸化炭素の排出量削減が大きな課題となっている。そのため、再生可能エネルギーへの注目が高まっている。太陽光発電や風力発電は、発電時に二酸化炭素をほとんど排出しないという特徴を持つ。しかし、天候によって発電量が変化するため、安定した供給が難しいという課題もある。そこで、大容量の蓄電池技術や送電技術の開発が進められている。電気を効率よく蓄え、必要な場所へ安定して届ける技術が発展すれば、再生可能エネルギーの利用はさらに広がっていくだろう。\n　宇宙開発の分野でも新たな動きが見られる。かつて宇宙開発は国家主導で行われることが多かったが、近年では民間企業の参入が進んでいる。再利用可能なロケットの開発によって打ち上げコストを下げる取り組みも進められており、宇宙旅行の実現を目指す企業も登場している。また、人工衛星は気象観測や通信だけでなく、災害時の状況確認にも利用されている。地震や大雨などの災害が発生した際には、人工衛星から得られる情報が救助活動や避難支援に役立つことがある。日本は自然災害が多い国であるため、こうした技術の重要性は今後さらに高まると考えられる。\n　このように、科学技術は私たちの生活を便利にし、多くの課題を解決する可能性を持っている。しかし、その技術をどのように利用するかによって、社会に与える影響は大きく変わる。便利だからといって無条件に受け入れるのではなく、メリットとデメリットの両方を理解した上で活用する姿勢が重要である。また、新しい技術を安全かつ適切に利用するためには、ルール作りや教育も欠かせない。特にこれからの時代を生きる高校生にとっては、単に技術を利用するだけでなく、その仕組みや影響について考える力が求められているのである。\n　今後も科学技術はさらに進歩していくと考えられる。十年前には想像できなかったような技術が、現在では当たり前のように利用されている。これから先、どのような技術が登場し、社会がどのように変化していくのかは誰にも正確には予測できない。しかし、科学技術が社会に大きな影響を与える存在であることは間違いない。だからこそ、私たちは新しい技術に関心を持ち、正しく理解しながら向き合っていく必要があるのである。"
  },
  {
    "id": "business-management-future",
    "title": "企業経営と変化するビジネス社会",
    "text": "　近年、企業活動を取り巻く環境は急速に変化している。特に情報技術の発展、国際競争の激化、消費者ニーズの多様化などにより、従来型の経営手法だけでは安定した成長を維持することが難しくなってきた。そのため、多くの企業では業務改善や組織改革を進めるとともに、柔軟な発想を持つ人材の育成に力を入れている。現代社会においては、単に利益を追求するだけではなく、社会的責任や持続可能性を意識した経営姿勢が求められているのである。\n　かつての企業経営では、大量生産と大量販売によって利益を拡大する方法が主流であった。しかし、人口減少や市場成熟化の影響により、現在では新たな価値を生み出すことが重要視されている。例えば、同じ商品を販売する場合であっても、価格競争だけではなく、顧客満足度や付加価値によって差別化を図る企業が増加している。さらに、商品そのものだけではなく、購入後のサポート体制や企業理念への共感なども、消費者が商品を選択する際の重要な判断材料となっている。\n　こうした状況の中で注目されているのが、データ活用による経営戦略である。企業は販売実績、顧客属性、購買履歴、在庫状況など、さまざまな情報を収集し、それらを分析することで経営判断に役立てている。例えば、過去の販売データを分析することで、需要の増加が予測される商品を事前に把握し、在庫不足を防ぐことが可能となる。また、顧客の年齢層や購入傾向を分析することで、広告戦略や新商品の開発方針を決定することもできる。近年では人工知能を活用した需要予測や自動応答システムの導入も進んでおり、業務効率化と人件費削減の両立が図られている。\n　しかし、情報技術を導入すれば必ず成功するわけではない。重要なのは、それらの技術を適切に運用できる人材を育成することである。実際、多くの企業ではデジタル化を進めたものの、従業員側が新しいシステムに対応できず、十分な成果を上げられない事例も存在している。そのため、社員研修や人材教育を継続的に実施し、組織全体として知識や技能を向上させる取り組みが必要となる。また、管理職には部下の意見を尊重しながら組織をまとめる能力が求められ、単なる指示命令型の経営スタイルから、対話重視型のマネジメントへと移行しつつある。\n　働き方改革についても、多くの企業が対応を迫られている。以前は長時間労働が当然とされる職場も少なくなかったが、近年では従業員の健康管理や生活環境への配慮が重視されるようになった。特に若年層では、給与水準だけではなく、休暇制度や勤務環境、福利厚生などを重視する傾向が強まっている。そのため、企業側も人材確保のために柔軟な勤務制度を整備している。例えば、在宅勤務や時差出勤制度を導入することで、多様な働き方に対応しようとする企業が増加しているのである。\n　さらに、企業には社会的責任を果たす姿勢も求められている。環境問題への対応として、省エネルギー化や廃棄物削減に取り組む企業が増加しているほか、地域社会との連携活動を積極的に行う事例も見られる。特に大企業では、二酸化炭素排出量の削減目標を公表し、再生可能エネルギーの活用を進めるなど、持続可能な社会の実現に向けた取り組みを強化している。また、災害発生時に地域住民へ物資を提供したり、避難場所として施設を開放したりするなど、地域貢献活動を重視する企業も増えている。\n　一方で、中小企業には別の課題も存在している。人材不足や後継者問題により、事業継続が困難になるケースが増加しているのである。特に地方では若年人口の流出が進み、採用活動が難航している企業も多い。そのため、外国人労働者の受け入れや、業務自動化による省人化などを進める必要性が高まっている。また、地域密着型企業の場合、大手企業との差別化を図るためには、地域特性を生かした商品開発や独自サービスの提供が重要となる。地域住民との信頼関係を構築し、長期的な顧客獲得につなげることが求められている。\n　今後の企業経営においては、変化への対応力がますます重要になると考えられる。市場環境や技術革新の速度が加速する現代社会では、一度成功した方法が将来も通用するとは限らない。そのため、経営者には状況変化を正確に分析し、迅速に意思決定を行う能力が必要である。また、従業員側にも主体的に学習を継続し、新しい知識や技能を習得する姿勢が求められる。企業と従業員が互いに協力しながら変化へ対応していくことが、持続的成長を実現するための重要な条件となるのである。"
  },
  {
    "id": "tourism-industry-future",
    "title": "観光業の発展と地域社会の未来",
    "text": "　近年、観光業は日本経済を支える重要な産業の１つとして注目されている。特に２０１０年代後半以降、訪日外国人観光客数は大幅に増加し、年間３０００万人を超える時期もあった。観光による経済効果は宿泊業や飲食業だけではなく、交通機関、小売業、農業、伝統工芸など多くの分野へ波及している。そのため、国や自治体は観光振興を地域活性化政策の中心に位置付けるようになったのである。\n　観光業が持つ特徴の１つは、地域固有の文化や自然資源を経済的価値へ転換できる点にある。例えば、歴史的建造物や伝統祭り、美しい自然景観などは、他地域には存在しない独自の魅力として観光資源になる。京都では寺院や神社を目的に多くの観光客が訪れ、北海道では広大な自然や冬季観光が人気を集めている。また、沖縄では海洋観光や独自文化が高い評価を受けている。このように、地域特性を生かした観光戦略は、地方経済の発展にも大きく関わっているのである。\n　観光業の発展を支えている要素として、交通機関の進歩も挙げられる。新幹線や航空路線の拡充によって、以前よりも短時間で移動できるようになった。例えば、東京から新大阪までは新幹線で約２時間３０分程度で移動できる。また、ＬＣＣと呼ばれる格安航空会社の普及により、若年層でも比較的安価に国内外へ旅行できる環境が整った。こうした交通インフラの整備は、観光客数増加に大きな影響を与えている。\n　さらに、近年では情報技術の発展が観光業を大きく変化させている。以前は旅行会社を通じて団体旅行を申し込む形式が一般的であったが、現在ではスマートフォンを利用して個人で旅行計画を立てる人が増加している。ＳＮＳでは観光地の写真や動画が瞬時に拡散され、「映える」と呼ばれる景観や料理が人気を集めるようになった。また、ＡＩを活用した多言語翻訳サービスや観光案内システムも普及し、外国人観光客が安心して旅行できる環境整備が進められている。\n　しかし、観光客増加には課題も存在している。その代表例が「オーバーツーリズム」である。特定の観光地に人が集中しすぎることで、交通渋滞や騒音問題、ゴミ問題などが発生している。例えば、有名観光地では通勤や通学に支障が出るほど混雑する場合もあり、地域住民の生活環境に悪影響を与えるケースが報告されている。また、観光客向け店舗が増加した結果、地元住民向けの商店が減少し、地域本来の生活文化が失われることへの懸念も広がっている。観光による利益を追求するだけではなく、地域社会との共存をどのように実現するかが大きな課題となっているのである。\n　２０２０年以降、新型感染症の拡大によって観光業は大きな打撃を受けた。各国で入国制限や移動制限が行われた結果、航空会社や宿泊施設、観光施設などは深刻な経営難に直面した。特に外国人観光客への依存度が高かった地域では、観光需要が急激に減少し、多くの事業者が経営継続の危機に陥った。しかし、この経験を通して国内観光需要の重要性が再認識されるようになったのである。感染状況が落ち着いた後には、近隣地域への旅行や自然体験型観光などが注目を集めるようになった。\n　近年では、ＳＤＧｓの考え方を観光業へ取り入れる動きも広がっている。これは単に観光客数を増やすだけではなく、環境保護や地域文化の維持を重視しながら持続可能な観光を目指す考え方である。例えば、自然公園では入域制限を設けて環境負荷を軽減したり、再利用可能な資源を活用した宿泊施設運営を行ったりする事例が増えている。また、地域住民が主体となって観光資源を管理し、観光収益を地域全体へ還元する仕組みづくりも進められている。\n　観光業においては人材育成も重要な課題となっている。観光業は接客を伴う産業であるため、語学力やコミュニケーション能力が求められる。また、宗教や文化の違いへの理解も必要である。例えば、食事制限への配慮や礼拝場所の確保など、多様な価値観に対応できる柔軟性が求められている。そのため、観光業界では英語研修や異文化理解教育を強化する企業が増加している。特に訪日外国人観光客への対応では、英語だけではなく、中国語や韓国語など複数言語への対応能力も重要視されている。\n　さらに、地方観光では独自性の確立が重要である。全国どこでも似たような観光施設や土産物ばかりでは、観光客へ強い印象を与えることは難しい。そのため、地域独自の歴史や文化、自然景観、食文化などを生かした観光戦略が必要となる。例えば、農業体験や漁業体験、伝統工芸体験など、地域住民との交流を重視した体験型観光が人気を集めている。また、地方鉄道や古民家を活用した観光事業なども注目されている。こうした地域密着型観光は、大量集客型観光とは異なる魅力を持っているのである。\n　今後、観光業はさらに国際化が進むと考えられる。航空技術や情報通信技術の発展によって、世界中の人々が以前より容易に海外旅行を行える時代となった。一方で、環境問題や人口減少、地域文化保護など、観光業が抱える課題も複雑化している。だからこそ、単なる利益追求ではなく、地域社会や自然環境との共生を重視した観光政策が必要となるのである。観光業は人々へ楽しみや感動を提供するだけではなく、地域経済や国際交流を支える重要な役割を担っているのである。"
  },
  {
    "id": "regional-culture-preservation",
    "title": "地域文化の継承と観光振興",
    "text": "　地域文化を支える活動の中には、長い時間をかけて受け継がれてきたものが数多く存在する。祭り、伝統芸能、工芸、食文化などは、その土地で暮らす人々の生活と深く結び付いており、単なる観光資源ではなく、地域社会そのものを形作る重要な要素となっている。しかし近年は人口減少や高齢化、生活様式の変化などによって、文化継承の在り方が大きく変化している。特に地方都市では、担い手不足が深刻化しており、従来と同じ形で文化を維持することが難しくなっている。その一方で、ＩＣＴやＳＮＳを活用した新しい保存方法や発信方法も広がっており、文化継承は新たな転換期を迎えている。\n　例えば、ある地方都市では、毎年夏に行われる伝統行事を維持するために、中学生や高校生を対象とした後継者育成講座を開設している。以前は地域住民だけで自然に技術や作法が継承されていたが、現在では指導体制を明確にしなければ継承が困難になっているからである。この地域では、太鼓演奏、踊り、衣装制作、会場設営などを体系的に学ぶ機会を用意し、年間で約１２０人の若者が参加しているという。参加者の中には、県外へ進学した後も長期休暇に帰省して活動に参加する者もおり、地域との結び付きが再確認される機会にもなっている。\n　文化継承において重要なのは、単に形式を残すことだけではない。なぜその文化が生まれ、どのような意味を持っているのかを理解することが必要である。例えば、農村地域で行われる収穫祭には、豊作への感謝だけではなく、地域共同体の結束を強める役割も存在していた。過去には、農作業を協力して行うことが生活維持に直結していたため、祭りを通して互いの信頼関係を確認する意味があったのである。しかし、機械化が進み、個人単位で農作業を行えるようになった現在では、祭りの役割そのものも変化している。単なる年中行事として実施するだけでは参加意欲が低下しやすく、地域全体で目的を再定義する必要が生じている。\n　そのため、近年では文化行事と観光振興を組み合わせる取り組みが増えている。観光客を呼び込むことによって経済効果を生み出し、その利益を文化保存へ活用する仕組みである。例えば、ある城下町では、伝統的な街並みと歴史文化を活用した夜間イベントを実施し、年間来訪者数が約１．８倍に増加したという。この地域では、地元高校生が英語による案内活動を担当し、外国人観光客との交流も行われている。さらに、ＶＲ技術を利用して過去の街並みを再現する試みも始まっており、観光と教育を結び付けた新しい文化体験として注目されている。\n　一方で、観光化が進み過ぎることによる問題点も指摘されている。本来は地域住民のために行われていた行事が、観光客向けの演出を優先するようになると、文化の本質が変化してしまう危険性がある。例えば、祭りの日程を大型連休に合わせて変更した結果、従来の宗教的意味合いが薄れてしまった事例も報告されている。また、写真撮影を目的とした観光客が増えたことで、地元住民が落ち着いて参加できなくなったという声も存在する。そのため、観光振興と文化保護をどのように両立させるかが重要な課題となっている。\n　文化保存に関連して、近年はＤＸ推進の動きも広がっている。ＤＸとは「Ｄｉｇｉｔａｌ　Ｔｒａｎｓｆｏｒｍａｔｉｏｎ」の略称であり、デジタル技術によって社会や組織を変革する考え方を指す。文化分野においても、古文書や映像資料をデータ化し、オンライン上で公開する取り組みが進められている。以前は地域の資料館へ行かなければ閲覧できなかった資料が、現在では自宅から確認できるようになり、研究活動の効率も向上している。また、３Ｄスキャン技術によって文化財を立体的に保存する事例も増えている。地震や火災などによって実物が損傷した場合でも、データとして形状を残すことが可能になるため、防災の観点からも注目されている。\n　さらに、映像配信サービスを活用した伝統芸能の公開も増加している。以前は会場へ足を運ばなければ鑑賞できなかった舞踊や演劇を、インターネット上で視聴できるようになったことで、若年層との接点が広がった。特に新型感染症の流行時には、多くの公演が中止となった一方で、オンライン配信によって活動を継続した団体も存在した。ある地方劇団では、有料配信によって約３万人の視聴者を集め、従来の会場公演を上回る収益を記録したという。このように、デジタル技術は文化活動の可能性を大きく広げている。\n　しかし、デジタル化だけで全ての問題が解決するわけではない。文化は本来、人と人との直接的な交流の中で継承される側面を持っている。祭りの熱気、舞台の空気感、工芸品を作る際の細かな感覚などは、映像だけでは完全に伝えることが難しい。そのため、実際の体験機会を維持することも重要である。最近では、学校教育の中で地域文化を学ぶ授業を取り入れる自治体も増えている。小学生が地域の伝統工芸を体験したり、高校生が郷土芸能の発表会を企画したりすることで、若い世代が文化へ関心を持つきっかけを作っているのである。\n　また、文化活動は世代間交流を促進する効果も持っている。高齢者が持つ知識や技術を若者へ伝える場が存在することで、地域社会のつながりが強化される。例えば、竹細工を伝承する団体では、７０代の職人が中学生へ制作方法を教える活動を続けており、年間約４００点の作品が制作されているという。参加した生徒からは、「地域にこんな技術が残っているとは知らなかった」という感想も多く寄せられている。このような交流は、単なる技術継承だけではなく、地域への愛着形成にもつながっている。\n　今後の文化継承を考える上では、「保存」と「変化」の両方を受け入れる視点が必要になるだろう。全てを昔のまま維持することは現実的ではないが、変化し過ぎれば文化本来の価値が失われる可能性もある。そのため、地域住民、行政、学校、企業などが協力し、それぞれの立場から支援を行うことが求められている。文化とは過去の遺産であると同時に、現在を生きる人々が未来へ受け渡していく存在でもある。社会環境が変化する中で、どのように文化を守り、発展させていくのか。その課題に向き合うことは、これからの地域社会において極めて重要な意味を持っている。"
  }
];

let textItems = [...TEXTS_FALLBACK];
let LONG_TEXT = TEXTS_FALLBACK[0].text;
let currentTextTitle = TEXTS_FALLBACK[0].title;
let currentTextId = TEXTS_FALLBACK[0].id;

let totalSeconds = 180;
let remainSeconds = 0;
let timerID = null;
let startTime = null;
let running = false;
let correctCount = 0;
let missCount = 0;

const timeSelect      = document.getElementById('time-select');
const taskTitle       = document.getElementById('task-title');
const startModeSelect = document.getElementById('start-mode');
const feedbackModeSelect = document.getElementById('feedback-mode');
// 結果画面は独立画面で常時表示するため、ホーム画面の結果表示選択は廃止。
const liveStatusModeSelect = document.getElementById('live-status-mode');
const themeModeSelect = document.getElementById('theme-mode');
const accessibilityModeSelect = document.getElementById('accessibility-mode');
const typingPositionModeSelect = document.getElementById('typing-position-mode');
const btnConfigToggle = document.getElementById('btn-config-toggle');
const advancedSettings = document.getElementById('advanced-settings');
const disqualifyLimitSelect = document.getElementById('disqualify-limit');
const btnStart        = document.getElementById('btn-start');
const btnAbort        = document.getElementById('btn-abort');
const btnRetry        = document.getElementById('btn-retry');
const btnRetryRandom  = document.getElementById('btn-retry-random');
const btnBackConfig   = document.getElementById('btn-back-config');
const timerDisplay    = document.getElementById('timer-display');
const timerLabel      = document.getElementById('timer-label');
const timerPill       = document.getElementById('timer-pill');
const correctDisplay  = document.getElementById('correct-display');
const missDisplay     = document.getElementById('miss-display');
const cpmDisplay      = document.getElementById('cpm-display');
const progressDisplay = document.getElementById('progress-display');
const progressBar     = document.getElementById('progress-bar');
const textDisplay     = document.getElementById('text-display');
const typingArea      = document.getElementById('typing-area');
const resultScreen    = document.getElementById('result-screen');
const resCorrect      = document.getElementById('res-correct');
const resMiss         = document.getElementById('res-miss');
const resAccuracy     = document.getElementById('res-accuracy');
const resCpm          = document.getElementById('res-cpm');
const resCps          = document.getElementById('res-cps');
const resElapsed      = document.getElementById('res-elapsed');
const resTaskTitle    = document.getElementById('res-task-title');
const resCondition    = document.getElementById('res-condition');
const resultSummaryText = document.getElementById('result-summary-text');
const countdownOverlay = document.getElementById('countdown-overlay');
const cpmChart        = document.getElementById('cpm-chart');

// --- 採点詳細関連の DOM 参照 -----------------------------------------------
// 結果画面に追加した「採点詳細」「表示設定」関連の要素をまとめて取得しておく。
// 既存ロジックには触れず、これらは endGame の最後と表示設定変更時にだけ使う。
const resInput            = document.getElementById('res-input');
const resErrorTotal       = document.getElementById('res-error-total');
const resNet              = document.getElementById('res-net');
const resDeduction        = document.getElementById('res-deduction');
const resJudge            = document.getElementById('res-judge');
const resJudgeNote        = document.getElementById('res-judge-note');
const errMisuseCount      = document.getElementById('err-misuse-count');
const errMissingCount     = document.getElementById('err-missing-count');
const errExtraCount       = document.getElementById('err-extra-count');
const errSpaceCount       = document.getElementById('err-space-count');
const errNewlineExtraCount= document.getElementById('err-newline-extra-count');
const errNewlineMissCount = document.getElementById('err-newline-miss-count');
const errWidthCount       = document.getElementById('err-width-count');
const errPunctCount       = document.getElementById('err-punct-count');
const listMisuse          = document.getElementById('list-misuse');
const listMissing         = document.getElementById('list-missing');
const listExtra           = document.getElementById('list-extra');
const listSpacing         = document.getElementById('list-spacing');
const listWidthPunct      = document.getElementById('list-width-punct');

// 1秒ごとの CPM 推移。{ time: 経過秒, cpm: その時点の CPM } を push する。
// time=0 (計測開始直後) は CPM=0 で初期化しておく。
let cpmHistory = [];
// 各秒のミス数履歴。CPMグラフにミス発生地点を控えめに表示するために使う。
let missHistory = [];
// グラフ上の点の座標。マウスホバー時の当たり判定に使う。
let cpmChartPoints = [];
let cpmChartHoverIndex = -1;
// 直前に記録した秒数。秒境界をまたいだときだけ push するために使う。
let lastRecordedSec = -1;

// カウントダウン中（スタート押下後〜計測開始前）に立つフラグ。
// 計測は始まっていないので running は false のままだが、
// 中断ボタンでキャンセルできるよう、別フラグで管理する。
let countingDown = false;
let countdownTimers = [];

// 毎回スタート時に課題文をランダム選択するため、
// 直前に出題した課題文IDを保持して同じ課題文の連続出題をできるだけ避ける。
let lastRandomTextId = null;
// 課題文の選択状態。通常は random、課題一覧で選んだ場合だけ manual にする。
let textSelectionMode = 'random';


function normalizeTextItem(item, index, genreInfo = null) {
  if (!item || typeof item.text !== 'string') return null;
  const title = typeof item.title === 'string' && item.title.trim()
    ? item.title.trim()
    : `課題文 ${index + 1}`;
  const baseId = typeof item.id === 'string' && item.id.trim()
    ? item.id.trim()
    : `text-${index + 1}`;
  const genre = typeof item.genre === 'string' && item.genre.trim()
    ? item.genre.trim()
    : (genreInfo && genreInfo.id ? genreInfo.id : 'other');
  const genreName = genreInfo && genreInfo.name ? genreInfo.name : genre;
  const length = typeof item.length === 'number' ? item.length : item.text.length;
  return { id: baseId, title, genre, genreName, length, text: item.text };
}

function dedupeTextIds(items) {
  const seen = new Map();
  return items.map((item, index) => {
    const count = seen.get(item.id) || 0;
    seen.set(item.id, count + 1);
    if (count === 0) return item;
    return { ...item, id: `${item.id}-${count + 1}` };
  });
}

async function loadTextsFromIndex() {
  const indexResponse = await fetch('data/index.json', { cache: 'no-store' });
  if (!indexResponse.ok) throw new Error(`data/index.json の読み込みに失敗しました: ${indexResponse.status}`);

  const categories = await indexResponse.json();
  if (!Array.isArray(categories) || categories.length === 0) {
    throw new Error('data/index.json に有効なカテゴリがありません。');
  }

  const loadedGroups = await Promise.all(categories.map(async (category) => {
    if (!category || typeof category.file !== 'string') return [];
    const response = await fetch(category.file, { cache: 'no-store' });
    if (!response.ok) throw new Error(`${category.file} の読み込みに失敗しました: ${response.status}`);
    const data = await response.json();
    const source = Array.isArray(data) ? data : data.texts;
    if (!Array.isArray(source)) return [];
    return source.map((item, index) => normalizeTextItem(item, index, category)).filter(Boolean);
  }));

  return dedupeTextIds(loadedGroups.flat());
}

async function loadTextsFromLegacyFile() {
  const response = await fetch('texts.json', { cache: 'no-store' });
  if (!response.ok) throw new Error(`texts.json の読み込みに失敗しました: ${response.status}`);
  const data = await response.json();
  const source = Array.isArray(data) ? data : data.texts;
  const loaded = Array.isArray(source)
    ? source.map((item, index) => normalizeTextItem(item, index)).filter(Boolean)
    : [];
  return dedupeTextIds(loaded);
}

const RANDOM_TEXT_VALUE = '__random__';

function populateTextSelect(items) {
  // 課題文プルダウンは廃止。課題選択は「課題一覧」モーダルに一本化する。
  // 既存の呼び出しとの互換性のため、関数名だけ残しておく。
}

function applySelectedText(textId, keepRandomSelection = false) {
  const selected = textItems.find(item => item.id === textId) || textItems[0];
  if (!selected) return;
  LONG_TEXT = selected.text;
  currentTextTitle = selected.title;
  currentTextId = selected.id;

  if (taskTitle) taskTitle.textContent = `// 課題文 — ${currentTextTitle}`;
  if (!running && !countingDown) {
    typingArea.value = '';
    resultScreen.style.display = 'none';
    initDisplay();
  }
}

// 登録済み課題文からランダムに1つ選ぶ。
// 2題以上ある場合は、直前と同じ課題文が連続しにくいようにする。
function applyRandomTextForStart() {
  if (!Array.isArray(textItems) || textItems.length === 0) return;

  let candidates = textItems;
  if (textItems.length > 1 && lastRandomTextId) {
    candidates = textItems.filter(item => item.id !== lastRandomTextId);
  }

  const selected = candidates[Math.floor(Math.random() * candidates.length)] || textItems[0];
  lastRandomTextId = selected.id;

  // 選択欄は「ランダム（毎回）」のままにして、実際の課題文だけを差し替える。
  applySelectedText(selected.id, true);
}

async function loadTexts() {
  populateTextSelect(textItems);
  applyRandomTextForStart();

  const loadedSources = [];

  try {
    const fromIndex = await loadTextsFromIndex();
    loadedSources.push(...fromIndex);
  } catch (indexError) {
    console.warn('ジャンル別JSONを読み込めませんでした。texts.json も確認します。', indexError);
  }

  // 旧 texts.json にだけ課題文を追加した場合でも取りこぼさないように、常に併読して統合する。
  try {
    const fromLegacy = await loadTextsFromLegacyFile();
    loadedSources.push(...fromLegacy);
  } catch (legacyError) {
    console.warn('texts.json を読み込めませんでした。', legacyError);
  }

  const loaded = dedupeTextIds(loadedSources);
  if (loaded.length > 0) {
    textItems = loaded;
    populateTextSelect(textItems);
    applyRandomTextForStart();
  } else {
    console.warn('外部JSONを読み込めないため、内蔵の課題文で起動します。');
  }
}



const btnTextLibrary = document.getElementById('btn-text-library');
const textLibraryModal = document.getElementById('text-library-modal');
const textLibraryList = document.getElementById('text-library-list');
const btnCloseLibrary = document.getElementById('btn-close-library');

// 課題一覧から選択したときに、表示本文・タイトル・選択状態を同期する。
function selectTextById(textId) {
  if (!textId || textId === RANDOM_TEXT_VALUE) {
    textSelectionMode = 'random';
    applyRandomTextForStart();
    return;
  }
  textSelectionMode = 'manual';
  applySelectedText(textId, false);
}

// 別画面の課題一覧。基本はランダム出題のまま、必要なときだけ手動選択できる。
function renderTextLibrary(items) {
  if (!textLibraryList) return;
  textLibraryList.innerHTML = '';

  if (!Array.isArray(items) || items.length === 0) {
    textLibraryList.innerHTML = '<p class="text-library-loading">表示できる課題文章がありません。</p>';
    return;
  }

  const randomButton = document.createElement('button');
  randomButton.type = 'button';
  randomButton.className = 'text-library-item text-library-random';
  randomButton.innerHTML = `
    <span class="text-library-title">ランダム（毎回）</span>
    <span class="text-library-meta">開始するたびに課題文章を自動で選びます。</span>
  `;
  randomButton.addEventListener('click', () => {
    textSelectionMode = 'random';
    applyRandomTextForStart();
    closeTextLibrary();
  });
  textLibraryList.appendChild(randomButton);

  items.forEach((item, index) => {
    const button = document.createElement('button');
    button.type = 'button';
    button.className = 'text-library-item';
    if (item.id === currentTextId && textSelectionMode === 'manual') {
      button.classList.add('is-selected');
    }

    const charCount = item.text ? item.text.length : 0;
    const genreLabel = item.genreName && item.genreName !== 'other' ? `${item.genreName}／` : '';
    const excerpt = (item.text || '').replace(/\s+/g, ' ').slice(0, 90);

    button.innerHTML = `
      <span class="text-library-title">${index + 1}. ${escapeHtml(item.title)}</span>
      <span class="text-library-meta">${escapeHtml(genreLabel)}${charCount.toLocaleString()}字程度</span>
      <span class="text-library-excerpt">${escapeHtml(excerpt)}${excerpt.length >= 90 ? '…' : ''}</span>
      <span class="text-library-select-label">この課題を選択</span>
    `;

    button.addEventListener('click', () => {
      selectTextById(item.id);
      closeTextLibrary();
    });
    textLibraryList.appendChild(button);
  });
}

function openTextLibrary() {
  if (!textLibraryModal) return;
  renderTextLibrary(textItems);
  textLibraryModal.classList.remove('hidden');
  textLibraryModal.setAttribute('aria-hidden', 'false');
}

function closeTextLibrary() {
  if (!textLibraryModal) return;
  textLibraryModal.classList.add('hidden');
  textLibraryModal.setAttribute('aria-hidden', 'true');
}

function escapeHtml(value) {
  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#39;');
}


function setConfigControlsDisabled(disabled) {
  timeSelect.disabled = disabled;
  startModeSelect.disabled = disabled;
  feedbackModeSelect.disabled = disabled;
  if (liveStatusModeSelect) liveStatusModeSelect.disabled = disabled;
  if (themeModeSelect) themeModeSelect.disabled = disabled;
  if (accessibilityModeSelect) accessibilityModeSelect.disabled = disabled;
  if (typingPositionModeSelect) typingPositionModeSelect.disabled = disabled;
  if (disqualifyLimitSelect) disqualifyLimitSelect.disabled = disabled;
  if (btnConfigToggle) btnConfigToggle.disabled = disabled;
}

function setAdvancedSettingsOpen(open) {
  if (!advancedSettings || !btnConfigToggle) return;
  advancedSettings.classList.toggle('is-collapsed', !open);
  btnConfigToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
  btnConfigToggle.textContent = open ? '詳細設定 ▴' : '詳細設定 ▾';
}


function lineNumberAt(text, index) {
  let line = 0;
  const end = Math.max(0, Math.min(index, text.length));
  for (let i = 0; i < end; i++) {
    if (text[i] === '\n') line++;
  }
  return line;
}


function isCompleteMode() {
  return timeSelect && timeSelect.value === 'complete';
}

function formatSeconds(sec) {
  const r = Math.max(0, Math.floor(sec));
  const m = Math.floor(r / 60);
  const s = r % 60;
  return `${m}:${s.toString().padStart(2,'0')}`;
}

function initDisplay() {
  document.body.classList.remove('result-mode');
  if (taskTitle) taskTitle.textContent = `// 課題文 — ${currentTextTitle}`;
  const completeMode = isCompleteMode();
  if (timerLabel) timerLabel.textContent = completeMode ? '経過' : 'TIME';
  if (completeMode) {
    timerDisplay.textContent = '0:00';
  } else {
    const secs = parseInt(timeSelect.value, 10) || 180;
    timerDisplay.textContent = formatSeconds(secs);
  }
  timerPill.classList.remove('danger');
  renderTextDisplay('');
  correctDisplay.textContent = '0';
  missDisplay.textContent = '0';
  cpmDisplay.textContent = '0';
  progressDisplay.textContent = '0%';
  progressBar.style.width = '0%';
  if (btnAbort) btnAbort.disabled = true;
}

timeSelect.addEventListener('change', () => { if (!running) initDisplay(); });

function renderTextDisplay(input, forceRevealErrors = false) {
  const target = LONG_TEXT;
  const showRealtimeMistakes = forceRevealErrors || !feedbackModeSelect || feedbackModeSelect.value === 'realtime';
  const showTypingPosition = !forceRevealErrors && typingPositionModeSelect && typingPositionModeSelect.value === 'show';
  let html = '';
  for (let i = 0; i < target.length; i++) {
    const ch = target[i];
    const isNewline = ch === '\n';
    // 改行は span 内に <br> を入れず、span の表示文字には半角スペース（カーソル位置で可視化するため）を使い、
    // span の直後に独立した <br> を置く。これによりカーソル span の高さやアニメーションが安定する。
    // 右側の累積文字数では、改行そのものは文字数に含めない。
    const disp = isNewline ? '&nbsp;' : escapeHtml(ch);
    const countAttr = isNewline ? ' data-countable="false"' : ' data-countable="true"';
    if (i < input.length) {
      if (input[i] === ch) {
        html += `<span class="char-typed" data-index="${i + 1}"${countAttr}>${disp}</span>`;
      } else {
        const cls = forceRevealErrors ? 'char-result-error' : (showRealtimeMistakes ? 'char-wrong' : 'char-typed');
        html += `<span class="${cls}" data-index="${i + 1}"${countAttr}>${disp}</span>`;
      }
    } else if (i === input.length) {
      const cls = showTypingPosition ? 'char-cursor' : 'char-untyped';
      html += `<span class="${cls}" data-index="${i + 1}" data-current-position="true"${countAttr}>${disp}</span>`;
    } else {
      html += `<span class="char-untyped" data-index="${i + 1}"${countAttr}>${disp}</span>`;
    }
    if (isNewline) html += '<br>';
  }
  textDisplay.innerHTML = `<div class="text-scroll-inner"><div id="text-content">${html}</div><div id="line-counts" aria-hidden="true"></div></div>`;

  // 文字を描画した後で、実際に画面上で折り返された各行を調べ、右側に累積文字数を表示する。
  requestAnimationFrame(rebuildLineCounters);

  // 入力が空（初期表示・リセット・ゲーム開始直後）は必ず一番上から見せる。
  // 入力が始まってから初めて、必要なときだけスクロール判定する。
  if (input.length === 0) {
    textDisplay.scrollTop = 0;
  } else {
    requestAnimationFrame(scrollToCursor);
  }
}

function rebuildLineCounters() {
  const content = document.getElementById('text-content');
  const counts = document.getElementById('line-counts');
  if (!content || !counts) return;

  const chars = Array.from(content.querySelectorAll('[data-index]'));
  const lines = [];

  chars.forEach(span => {
    // 改行記号そのものは右側の文字数に含めない。
    if (span.dataset.countable === 'false') return;
    const top = Math.round(span.offsetTop);
    let line = lines.find(item => Math.abs(item.top - top) <= 2);
    if (!line) {
      line = { top, count: 0 };
      lines.push(line);
    }
    line.count += 1;
  });

  lines.sort((a, b) => a.top - b.top);
  let cumulative = 0;
  counts.innerHTML = lines.map(line => {
    cumulative += line.count;
    return `<span class="line-count" style="top:${line.top}px">${cumulative}</span>`;
  }).join('');
}


let lineCounterResizeTimer = null;
window.addEventListener('resize', () => {
  clearTimeout(lineCounterResizeTimer);
  lineCounterResizeTimer = setTimeout(rebuildLineCounters, 120);
});

function scrollToCursor() {
  const cursor = textDisplay.querySelector('[data-current-position="true"], .char-cursor');
  if (!cursor) return;

  const wrapH = textDisplay.clientHeight;
  const scrollTop = textDisplay.scrollTop;

  // offsetTop は offsetParent 基準になり、親要素の位置が混ざって
  // 冒頭1文字目でも「下にはみ出した」と誤判定することがある。
  // getBoundingClientRect() で textDisplay 内部での相対位置に直してから判定する。
  const displayRect = textDisplay.getBoundingClientRect();
  const cursorRect = cursor.getBoundingClientRect();
  const cursorTop = cursorRect.top - displayRect.top + scrollTop;
  const cursorBottom = cursorRect.bottom - displayRect.top + scrollTop;

  const viewTop = scrollTop;
  const viewBottom = scrollTop + wrapH;

  // 現在行だけでなく、次の行も見えるようにする。
  // line-height は CSS の実値を使い、取得できない場合はカーソルの高さから推定する。
  const computed = getComputedStyle(textDisplay);
  const parsedLineHeight = parseFloat(computed.lineHeight);
  const cursorHeight = Math.max(1, cursorRect.height);
  const lineHeight = Number.isFinite(parsedLineHeight) ? parsedLineHeight : cursorHeight * 2.1;

  // 「カーソルの下に次の1行分の余白」が残っていれば、次の行が見える。
  // ただし小さい画面で極端に余白を取りすぎないよう、表示欄の45%を上限にする。
  const nextLineMargin = Math.min(lineHeight * 1.25, wrapH * 0.45);
  const desiredBottom = cursorBottom + nextLineMargin;

  // カーソル行と次行が表示範囲に収まっているなら何もしない。
  if (cursorTop >= viewTop && desiredBottom <= viewBottom) {
    return;
  }
  // カーソルが下側に近づいた場合、次の行が見える位置まで最小限スクロールする。
  if (desiredBottom > viewBottom) {
    textDisplay.scrollTop = desiredBottom - wrapH;
    return;
  }
  // カーソルが上にはみ出した場合、カーソルが上端に来るよう戻す。
  if (cursorTop < viewTop) {
    textDisplay.scrollTop = cursorTop;
  }
}

function updateStats(input) {
  const target = LONG_TEXT;
  let correct = 0, miss = 0;
  const len = Math.min(input.length, target.length);
  for (let i = 0; i < len; i++) {
    if (input[i] === target[i]) correct++;
    else miss++;
  }
  if (input.length > target.length) miss += input.length - target.length;
  correctCount = correct;
  missCount = miss;
  const elapsed = Math.max(1, (Date.now() - startTime) / 1000);
  const cpm = Math.round((correct / elapsed) * 60);
  const pct = Math.round((input.length / target.length) * 100);
  correctDisplay.textContent = correct;
  missDisplay.textContent = miss;
  cpmDisplay.textContent = cpm;
  progressDisplay.textContent = `${Math.min(pct, 100)}%`;
  progressBar.style.width = `${Math.min(pct, 100)}%`;
}

function updateTimer() {
  if (isCompleteMode()) {
    const elapsed = startTime ? (Date.now() - startTime) / 1000 : 0;
    if (timerLabel) timerLabel.textContent = '経過';
    timerDisplay.textContent = formatSeconds(elapsed);
    timerPill.classList.remove('danger');
    return;
  }
  const r = Math.max(0, remainSeconds);
  if (timerLabel) timerLabel.textContent = 'TIME';
  timerDisplay.textContent = formatSeconds(r);
  if (r <= 10) timerPill.classList.add('danger');
  else timerPill.classList.remove('danger');
}

// スタートボタンのエントリポイント。
// 設定値（制限時間・開始タイミング）を読み取って、
// 「すぐ開始」なら即座に計測を始め、「3秒後に開始」なら
// カウントダウン演出を挟んでから計測を始める。
function startGame() {
  // 基本仕様はランダム出題。課題一覧で明示的に選んだ場合だけ、その課題を使う。
  // Escキーで開始した場合もこの startGame() を通るため、同じ仕様になる。
  if (textSelectionMode === 'random') {
    applyRandomTextForStart();
  } else if (currentTextId) {
    applySelectedText(currentTextId);
  }

  const completeMode = isCompleteMode();
  totalSeconds = completeMode ? 0 : (parseInt(timeSelect.value, 10) || 180);
  const mode = startModeSelect.value;

  // モード共通の前準備（結果画面を閉じる、課題文をリセット表示する、など）。
  // ただし入力欄を有効化するかどうかはモードごとに違うので、ここではまだ触らない。
  resultScreen.style.display = 'none';
  correctCount = 0;
  missCount = 0;
  remainSeconds = totalSeconds;
  renderTextDisplay('');
  correctDisplay.textContent = '0';
  missDisplay.textContent = '0';
  cpmDisplay.textContent = '0';
  progressDisplay.textContent = '0%';
  progressBar.style.width = '0%';
  timerPill.classList.remove('danger');
  updateTimer();

  if (mode === 'countdown') {
    runCountdown(() => beginMeasurement());
  } else {
    beginMeasurement();
  }
}

// 3,2,1,START のカウントダウンを表示し、終わったら onDone を呼ぶ。
// この間は入力欄を有効にせず、制限時間タイマーも動かさない。
// 中断ボタンはこのフェーズでも押せるようにしておく。
function runCountdown(onDone) {
  countingDown = true;
  // ボタン状態: スタートは押せない、中断はカウントダウンを取り消すために有効。
  btnStart.disabled = true;
  btnAbort.disabled = false;
  setConfigControlsDisabled(true);
  // 入力欄はまだ無効のまま
  typingArea.disabled = true;
  typingArea.value = '';

  // オーバーレイ表示
  countdownOverlay.classList.add('active');

  const steps = [
    { label: '3', go: false, delay: 0 },
    { label: '2', go: false, delay: 1000 },
    { label: '1', go: false, delay: 2000 },
    { label: 'START', go: true, delay: 3000 },
  ];

  countdownTimers = [];
  steps.forEach(step => {
    const t = setTimeout(() => {
      // アニメーションをやり直すため、毎回新しい span に差し替える。
      const current = countdownOverlay.querySelector('.cd-num');
      const fresh = document.createElement('span');
      fresh.id = 'countdown-num';
      fresh.textContent = step.label;
      fresh.className = step.go ? 'cd-num go' : 'cd-num';
      if (current) current.replaceWith(fresh);
      else countdownOverlay.appendChild(fresh);
    }, step.delay);
    countdownTimers.push(t);
  });
  // 最後の START 表示から少し見せた後、計測開始
  const finishT = setTimeout(() => {
    countdownOverlay.classList.remove('active');
    countingDown = false;
    countdownTimers = [];
    onDone();
  }, 3500);
  countdownTimers.push(finishT);
}

// カウントダウン中に中断ボタンが押されたときの取消処理。
// 計測は始まっていないので採点はせず、スタート前の状態へ戻すだけ。
function cancelCountdown() {
  countdownTimers.forEach(clearTimeout);
  countdownTimers = [];
  countingDown = false;
  countdownOverlay.classList.remove('active');
  document.body.classList.remove('focus-mode');
  // 初期状態（スタート前）に戻す
  btnStart.disabled = false;
  btnAbort.disabled = true;
  setConfigControlsDisabled(false);
  typingArea.disabled = true;
  typingArea.value = '';
  initDisplay();
}

// 実際に計測を開始する処理。startTime をここで取得することで、
// CPM の計算もカウントダウン終了後の時刻を基準にできる。
function beginMeasurement() {
  const completeMode = isCompleteMode();
  totalSeconds = completeMode ? 0 : (parseInt(timeSelect.value, 10) || 180);
  remainSeconds = totalSeconds;
  correctCount = 0;
  missCount = 0;
  startTime = Date.now();
  running = true;
  document.body.classList.add('focus-mode');
  // CPM 履歴をリセット。0秒時点は CPM=0 として起点を持たせておくと、
  // 折れ線が左端から立ち上がるのできれいに見える。
  cpmHistory = [{ time: 0, cpm: 0, correct: 0, instantCpm: 0 }];
  missHistory = [{ time: 0, miss: 0 }];
  lastRecordedSec = 0;
  typingArea.value = '';
  typingArea.disabled = false;
  typingArea.focus();
  btnStart.disabled = true;
  btnAbort.disabled = false;
  setConfigControlsDisabled(true);
  resultScreen.style.display = 'none';
  renderTextDisplay('');
  updateStats('');
  updateTimer();
  timerID = setInterval(() => {
    const elapsed = (Date.now() - startTime) / 1000;
    if (!isCompleteMode()) {
      remainSeconds = totalSeconds - Math.floor(elapsed);
    }
    updateTimer();
    updateStats(typingArea.value);
    // 秒境界をまたいだときだけ CPM を記録する。
    // setInterval は 250ms 間隔なので、Math.floor を見て1秒進んだら push する。
    const sec = Math.floor(elapsed);
    if (sec > lastRecordedSec) {
      const cpmNow = parseInt(cpmDisplay.textContent, 10) || 0;
      const prevPoint = cpmHistory[cpmHistory.length - 1] || { time: 0, correct: 0 };
      const diffTime = Math.max(1, sec - prevPoint.time);
      const diffCorrect = Math.max(0, correctCount - (prevPoint.correct || 0));
      const instantCpm = Math.round((diffCorrect / diffTime) * 60);
      cpmHistory.push({ time: sec, cpm: cpmNow, correct: correctCount, instantCpm, miss: missCount });
      missHistory.push({ time: sec, miss: missCount });
      lastRecordedSec = sec;
    }
    if (!isCompleteMode() && remainSeconds <= 0) endGame();
  }, 250);
}

function endGame() {
  if (!running) return;
  running = false;
  document.body.classList.remove('focus-mode');
  clearInterval(timerID);

  // 終了ボタン直後やIME確定直後でも、最後の入力内容で必ず再集計する。
  // これにより、結果画面の基本数値と詳細採点の入力範囲がずれない。
  const finalInput = typingArea.value;
  updateStats(finalInput);

  typingArea.disabled = true;
  btnStart.disabled = false;
  btnAbort.disabled = true;
  setConfigControlsDisabled(false);
  const elapsed = Math.max(1, (Date.now() - startTime) / 1000);
  const total = correctCount + missCount;
  const accuracy = total > 0 ? Math.round((correctCount / total) * 100) : 0;
  const cpm = Math.round((correctCount / elapsed) * 60);
  const cps = (correctCount / elapsed).toFixed(1);
  resCorrect.textContent = correctCount;
  resMiss.textContent = missCount;
  resAccuracy.textContent = accuracy;
  resCpm.textContent = cpm;
  resCps.textContent = cps;
  if (resElapsed) resElapsed.textContent = formatSeconds(Math.floor(elapsed));
  if (resTaskTitle) resTaskTitle.textContent = `課題文：${currentTextTitle}`;
  if (resCondition) {
    resCondition.textContent = isCompleteMode()
      ? '終了条件：全文打ち切り'
      : `終了条件：${formatSeconds(parseInt(timeSelect.value, 10) || 180)}`;
  }
  if (resultSummaryText) {
    resultSummaryText.textContent = `正解 ${correctCount} 文字、ミス ${missCount} 回、正確率 ${accuracy}%、CPM ${cpm}。`;
  }
  // 最終時点の CPM を履歴の末尾に追加して、グラフの右端をきっちり最終値で終わらせる。
  // 例えば 30 秒で終了した場合、最後の秒境界記録（時刻 30 のはず）の上に
  // 同じ秒の最終値を上書きすると重複するので、末尾と同じ秒なら置換する。
  const lastSec = Math.floor(elapsed);
  const prevFinalPoint = cpmHistory.length ? cpmHistory[cpmHistory.length - 1] : { time: 0, correct: 0 };
  const diffFinalTime = Math.max(1, lastSec - prevFinalPoint.time);
  const diffFinalCorrect = Math.max(0, correctCount - (prevFinalPoint.correct || 0));
  const finalInstantCpm = diffFinalCorrect > 0 ? Math.round((diffFinalCorrect / diffFinalTime) * 60) : (prevFinalPoint.instantCpm || 0);
  const finalPoint = { time: lastSec, cpm, correct: correctCount, instantCpm: finalInstantCpm, miss: missCount };
  if (cpmHistory.length && cpmHistory[cpmHistory.length - 1].time === lastSec) {
    cpmHistory[cpmHistory.length - 1] = finalPoint;
    if (missHistory.length) missHistory[missHistory.length - 1] = { time: lastSec, miss: missCount };
  } else {
    cpmHistory.push(finalPoint);
    missHistory.push({ time: lastSec, miss: missCount });
  }
  document.body.classList.add('result-mode');
  resultScreen.style.display = 'block';
  window.scrollTo({ top: 0, behavior: 'smooth' });
  // 結果画面を表示してから描画する。display:none の状態だと canvas の
  // clientWidth が 0 になり、解像度合わせがずれるため。
  cpmChartHoverIndex = -1;
  drawCPMChart();
  // 採点詳細（誤字・脱字・余字・空白・改行・全角半角・句読点）を計算して
  // 結果画面の各セクションに反映する。表示／非表示は別途チェックボックスで制御する。
  runDetailedScoring(finalInput);
  if (feedbackModeSelect && feedbackModeSelect.value === 'result') {
    renderTextDisplay(finalInput, true);
  }
}


// === 不正入力防止 =========================================================
// 速度試験では、コピー＆ペーストや外部テキストのドラッグ投入を禁止したい。
// ブラウザ上で完全な不正防止はできないが、通常操作での貼り付けはここでまとめて抑止する。
// 対象:
//  - Ctrl+V / Cmd+V / Shift+Insert
//  - 右クリックメニューからの貼り付け
//  - paste イベント
//  - beforeinput の insertFromPaste / insertFromDrop
//  - ドラッグ＆ドロップによる文字投入
let blockedPasteCount = 0;

function showAntiCheatNotice(message) {
  blockedPasteCount++;
  const base = '貼り付け・ドラッグ投入は禁止です。手入力のみで行ってください。';
  const text = message || base;
  // 入力中に何度も alert が出ると操作性が悪いため、画面上のプレースホルダで知らせる。
  typingArea.placeholder = `${text}（検知 ${blockedPasteCount} 回）`;
}

function blockIllegalInput(e, message) {
  e.preventDefault();
  e.stopPropagation();
  showAntiCheatNotice(message);
  return false;
}

typingArea.addEventListener('paste', (e) => {
  blockIllegalInput(e);
});

typingArea.addEventListener('drop', (e) => {
  blockIllegalInput(e, 'ドラッグ＆ドロップによる入力は禁止です。手入力のみで行ってください。');
});

typingArea.addEventListener('dragover', (e) => {
  // drop を成立させないため、dragover も止めておく。
  e.preventDefault();
});

typingArea.addEventListener('contextmenu', (e) => {
  // 右クリックメニューからの貼り付けを抑止。
  blockIllegalInput(e, '右クリックメニューからの貼り付けは禁止です。');
});

typingArea.addEventListener('beforeinput', (e) => {
  if (e.inputType === 'insertFromPaste' || e.inputType === 'insertFromDrop') {
    blockIllegalInput(e);
  }
});

// IME変換中フラグ。日本語をローマ字入力する際、未確定文字（例: "d" や "n"）も
// textarea.value に入り input イベントが発火するため、変換中は表示・統計の更新を止める。
// 確定したタイミング（compositionend）で改めて更新する。
let isComposing = false;

typingArea.addEventListener('compositionstart', () => {
  isComposing = true;
});

// IME変換中の Enter キーが textarea に改行として挿入されるのを防ぐ。
// 一部のブラウザ／IMEでは変換確定のEnterが keydown として漏れて、textarea に \n を挿入し、
// その結果カーソル位置が課題文側で1つ余計に進んで自動スクロールしてしまう。
// keydown は compositionend より先に届くケースが多いため、event.isComposing / keyCode===229 をチェック。
typingArea.addEventListener('keydown', (e) => {
  const key = e.key.toLowerCase();

  // 貼り付けショートカットを禁止する。
  // Ctrl+V / Cmd+V に加え、Windowsで使われる Shift+Insert も止める。
  if (((e.ctrlKey || e.metaKey) && key === 'v') || (e.shiftKey && e.key === 'Insert')) {
    blockIllegalInput(e);
    return;
  }

  if (e.key === 'Enter' && (e.isComposing || e.keyCode === 229)) {
    e.preventDefault();
  }
});

typingArea.addEventListener('compositionend', () => {
  isComposing = false;
  if (!running) return;
  // 念のため、確定直後に末尾の改行が紛れ込んでいたら除去する（保険）。
  if (typingArea.value.endsWith('\n')) {
    typingArea.value = typingArea.value.replace(/\n+$/, '');
  }
  const input = typingArea.value;
  renderTextDisplay(input);
  updateStats(input);
  if (input.length >= LONG_TEXT.length && (isCompleteMode() || input === LONG_TEXT)) endGame();
});

typingArea.addEventListener('input', (e) => {
  if (!running) return;
  // Chrome/Edge では input イベント側にも isComposing が立つことがある。
  // 「d」など未確定のローマ字1文字で課題文側を動かさないため、両方を見る。
  if (isComposing || e.isComposing) return; // 変換中は無視
  const input = typingArea.value;
  renderTextDisplay(input);
  updateStats(input);
  if (input.length >= LONG_TEXT.length && (isCompleteMode() || input === LONG_TEXT)) endGame();
});


// === キーボードショートカット =============================================
// Esc: 未開始ならスタート、カウントダウン中ならキャンセル、計測中なら中断して採点。
// Enter は日本語入力の変換確定や改行入力と衝突しやすいため、開始キーとしては使わない。
document.addEventListener('keydown', (e) => {
  if (e.isComposing || e.keyCode === 229) return;

  if (e.key !== 'Escape') return;

  e.preventDefault();

  if (countingDown) {
    cancelCountdown();
    return;
  }

  if (running) {
    endGame();
    return;
  }

  const resultVisible = resultScreen && resultScreen.style.display === 'block';
  if (resultVisible) return;

  startGame();
});

btnStart.addEventListener('click', startGame);
btnAbort.addEventListener('click', () => {
  // カウントダウン中なら計測を始める前の状態に戻すだけ。
  // 計測中なら従来通り「中断して採点」へ。
  if (countingDown) {
    cancelCountdown();
    return;
  }
  if (running) endGame();
});
function closeResultScreenForNextPractice() {
  // 結果画面から戻るときは、必ず「スタート前」の状態に戻す。
  // 再挑戦ボタンを押した直後に、前回のタイマーや入力可能状態が残って
  // そのまま計測が始まったように見えることを防ぐ。
  running = false;
  countingDown = false;
  clearInterval(timerID);
  timerID = null;
  countdownTimers.forEach(t => clearTimeout(t));
  countdownTimers = [];

  document.body.classList.remove('focus-mode', 'result-mode');
  if (countdownOverlay) countdownOverlay.classList.remove('active');
  if (resultScreen) resultScreen.style.display = 'none';
  if (typingArea) {
    typingArea.value = '';
    typingArea.disabled = true;
    typingArea.blur();
  }

  if (btnStart) btnStart.disabled = false;
  if (btnAbort) btnAbort.disabled = true;
  setConfigControlsDisabled(false);

  initDisplay();
  window.scrollTo({ top: 0, behavior: 'smooth' });
}

function restartSameText() {
  if (currentTextId) {
    // 「ランダム」で出た課題でも、同じ課題を確実に再利用できるように手動選択扱いへ切り替える。
    textSelectionMode = 'manual';
    applySelectedText(currentTextId);
  }
  // 再挑戦ボタンでは計測を開始しない。
  // 課題文をスタート前の状態に戻し、開始は通常のスタートボタンまたはEscキーに任せる。
  closeResultScreenForNextPractice();
}

function restartRandomText() {
  textSelectionMode = 'random';
  // ランダム課題を先に表示するだけで、計測は開始しない。
  // いきなりカウントダウンや計測が始まらないようにする。
  applyRandomTextForStart();
  closeResultScreenForNextPractice();
}

if (btnRetry) btnRetry.addEventListener('click', (e) => {
  e.preventDefault();
  e.stopPropagation();
  restartSameText();
  btnRetry.blur();
});
if (btnRetryRandom) btnRetryRandom.addEventListener('click', (e) => {
  e.preventDefault();
  e.stopPropagation();
  restartRandomText();
  btnRetryRandom.blur();
});
if (btnBackConfig) btnBackConfig.addEventListener('click', (e) => {
  e.preventDefault();
  e.stopPropagation();
  closeResultScreenForNextPractice();
  btnBackConfig.blur();
});

// === CPM 推移グラフ =========================================================
// 仕様:
//  - 横軸 = 経過秒数（0 〜 cpmHistory の最後の time）
//  - 縦軸 = CPM（0 〜 最大値を切り上げたキリのいい数字までで自動スケール）
//  - グリッド、軸ラベル、折れ線、データ点ドットを描画
//  - CSS で見た目の幅は親に任せ、Canvas のバッファは devicePixelRatio に合わせる
function drawCPMChart(hoverIndex = -1) {
  const canvas = cpmChart;
  if (!canvas || !cpmHistory || cpmHistory.length === 0) return;

  // CSS 上のサイズ（px）と DPR を取り、内部バッファを高解像度に。
  const dpr = window.devicePixelRatio || 1;
  const cssW = canvas.clientWidth;
  const cssH = canvas.clientHeight;
  if (cssW === 0 || cssH === 0) return; // 非表示中などのガード
  canvas.width  = Math.floor(cssW * dpr);
  canvas.height = Math.floor(cssH * dpr);

  const ctx = canvas.getContext('2d');
  ctx.setTransform(dpr, 0, 0, dpr, 0, 0); // 以降の描画は CSS 座標で書ける
  ctx.clearRect(0, 0, cssW, cssH);
  cpmChartPoints = [];

  // テーマ色（:root の CSS 変数から取得して、テーマ変更にも追従させる）
  const styles = getComputedStyle(document.body);
  const colAccent  = styles.getPropertyValue('--accent').trim()   || '#4fd1c5';
  const colAccent2 = styles.getPropertyValue('--accent2').trim()  || '#f6ad55';
  const colBorder  = styles.getPropertyValue('--border').trim()   || '#2a3045';
  const colDim     = styles.getPropertyValue('--text-dim').trim() || '#718096';

  // 描画領域の余白（軸ラベルのスペース）
  const padL = 44; // 左: Y 軸ラベル
  const padR = 14;
  const padT = 12;
  const padB = 28; // 下: X 軸ラベル
  const plotW = cssW - padL - padR;
  const plotH = cssH - padT - padB;
  if (plotW <= 0 || plotH <= 0) return;

  // データの範囲
  const maxTime = Math.max(1, cpmHistory[cpmHistory.length - 1].time);
  const rawMaxCpm = cpmHistory.reduce((m, p) => Math.max(m, p.cpm), 0);
  // 縦軸の上端は、最大値より少し上のキリのいい数字に切り上げる。
  // 例: 最大 173 なら 200、最大 38 なら 50、最大 0 なら 60。
  const niceMax = niceCeil(rawMaxCpm > 0 ? rawMaxCpm * 1.1 : 60);

  // 座標変換
  const xOf = (t) => padL + (t / maxTime) * plotW;
  const yOf = (c) => padT + plotH - (c / niceMax) * plotH;

  // ホバー判定用に、各データ点の canvas 上の座標を保存しておく。
  cpmChartPoints = cpmHistory.map((p, index) => ({
    index,
    time: p.time,
    avgCpm: p.cpm,
    instantCpm: typeof p.instantCpm === 'number' ? p.instantCpm : p.cpm,
    miss: typeof p.miss === 'number' ? p.miss : 0,
    x: xOf(p.time),
    y: yOf(p.cpm)
  }));

  // --- グリッド & 軸ラベル ---
  ctx.lineWidth = 1;
  ctx.font = '10px "Share Tech Mono", monospace';
  ctx.fillStyle = colDim;

  // 横グリッド（Y 方向、CPM のキリの良い目盛り）。5 等分。
  const ySteps = 5;
  ctx.strokeStyle = colBorder;
  for (let i = 0; i <= ySteps; i++) {
    const v = (niceMax / ySteps) * i;
    const y = yOf(v);
    ctx.globalAlpha = i === 0 ? 0.8 : 0.35; // 軸線(底辺)だけ濃く
    ctx.beginPath();
    ctx.moveTo(padL, y);
    ctx.lineTo(padL + plotW, y);
    ctx.stroke();
    ctx.globalAlpha = 1;
    ctx.textAlign = 'right';
    ctx.textBaseline = 'middle';
    ctx.fillText(String(Math.round(v)), padL - 6, y);
  }

  // 縦グリッド（X 方向、経過秒数）。最大時間を見て妥当な間隔を選ぶ。
  const xStep = chooseTimeStep(maxTime);
  ctx.strokeStyle = colBorder;
  ctx.textAlign = 'center';
  ctx.textBaseline = 'top';
  for (let t = 0; t <= maxTime + 0.0001; t += xStep) {
    const x = xOf(t);
    ctx.globalAlpha = t === 0 ? 0.8 : 0.25;
    ctx.beginPath();
    ctx.moveTo(x, padT);
    ctx.lineTo(x, padT + plotH);
    ctx.stroke();
    ctx.globalAlpha = 1;
    ctx.fillText(formatSec(t), x, padT + plotH + 6);
  }

  // 軸の左端線（縦軸）
  ctx.strokeStyle = colBorder;
  ctx.globalAlpha = 0.8;
  ctx.beginPath();
  ctx.moveTo(padL, padT);
  ctx.lineTo(padL, padT + plotH);
  ctx.stroke();
  ctx.globalAlpha = 1;

  // --- 平均CPMの補助線（最終平均値） ---
  const finalAvgCpm = cpmHistory[cpmHistory.length - 1]?.cpm || 0;
  if (finalAvgCpm > 0) {
    const avgY = yOf(finalAvgCpm);
    ctx.save();
    ctx.strokeStyle = colAccent2;
    ctx.globalAlpha = 0.55;
    ctx.setLineDash([5, 5]);
    ctx.beginPath();
    ctx.moveTo(padL, avgY);
    ctx.lineTo(padL + plotW, avgY);
    ctx.stroke();
    ctx.setLineDash([]);
    ctx.font = '10px "Share Tech Mono", monospace';
    ctx.fillStyle = colAccent2;
    ctx.textAlign = 'right';
    ctx.textBaseline = 'bottom';
    ctx.fillText(`AVG ${finalAvgCpm}`, padL + plotW - 4, avgY - 4);
    ctx.restore();
  }

  // --- 折れ線塗りつぶし（アクセントカラーのグラデで薄く） ---
  if (cpmHistory.length >= 2) {
    const grad = ctx.createLinearGradient(0, padT, 0, padT + plotH);
    grad.addColorStop(0, hexToRgba(colAccent, 0.32));
    grad.addColorStop(1, hexToRgba(colAccent, 0.02));
    ctx.fillStyle = grad;
    ctx.beginPath();
    ctx.moveTo(xOf(cpmHistory[0].time), padT + plotH);
    cpmHistory.forEach(p => ctx.lineTo(xOf(p.time), yOf(p.cpm)));
    ctx.lineTo(xOf(cpmHistory[cpmHistory.length - 1].time), padT + plotH);
    ctx.closePath();
    ctx.fill();
  }

  // --- 折れ線本体 ---
  ctx.strokeStyle = colAccent;
  ctx.lineWidth = 2;
  ctx.lineJoin = 'round';
  ctx.lineCap = 'round';
  ctx.beginPath();
  cpmHistory.forEach((p, i) => {
    const x = xOf(p.time);
    const y = yOf(p.cpm);
    if (i === 0) ctx.moveTo(x, y);
    else ctx.lineTo(x, y);
  });
  ctx.stroke();

  // --- データ点ドット ---
  // 点が多すぎると団子になるので、5秒以上の計測のときは間引く。
  const dotEvery = cpmHistory.length > 60 ? Math.ceil(cpmHistory.length / 60) : 1;
  ctx.fillStyle = colAccent;
  cpmHistory.forEach((p, i) => {
    if (i % dotEvery !== 0 && i !== cpmHistory.length - 1) return;
    ctx.beginPath();
    ctx.arc(xOf(p.time), yOf(p.cpm), 2.2, 0, Math.PI * 2);
    ctx.fill();
  });

  // --- ミス発生地点マーカー ---
  // 色だけに頼らず、下部に「×」マーカーを表示する。
  ctx.save();
  ctx.strokeStyle = styles.getPropertyValue('--wrong').trim() || '#d55e00';
  ctx.lineWidth = 2;
  ctx.globalAlpha = 0.95;
  for (let i = 1; i < cpmHistory.length; i++) {
    const prevMiss = typeof cpmHistory[i - 1].miss === 'number' ? cpmHistory[i - 1].miss : 0;
    const nowMiss = typeof cpmHistory[i].miss === 'number' ? cpmHistory[i].miss : prevMiss;
    if (nowMiss > prevMiss) {
      const x = xOf(cpmHistory[i].time);
      const y = padT + plotH - 5;
      ctx.beginPath();
      ctx.moveTo(x - 4, y - 4);
      ctx.lineTo(x + 4, y + 4);
      ctx.moveTo(x + 4, y - 4);
      ctx.lineTo(x - 4, y + 4);
      ctx.stroke();
    }
  }
  ctx.restore();

  // --- 最終ポイントを強調 ---
  const last = cpmHistory[cpmHistory.length - 1];
  ctx.fillStyle = colAccent2;
  ctx.beginPath();
  ctx.arc(xOf(last.time), yOf(last.cpm), 3.5, 0, Math.PI * 2);
  ctx.fill();

  // --- ホバー中の点とツールチップ ---
  if (hoverIndex >= 0 && cpmChartPoints[hoverIndex]) {
    drawCPMTooltip(ctx, cpmChartPoints[hoverIndex], cssW, cssH, colAccent2, colBorder);
  }
}

// グラフの点にマウスポインタを重ねたときに表示するツールチップ。
// 瞬間CPM = 直前の記録点からその点までの1秒ごとの速度、平均CPM = 開始からその点までの平均速度。
function drawCPMTooltip(ctx, point, cssW, cssH, colAccent2, colBorder) {
  const lines = [
    `時刻: ${formatSec(point.time)}`,
    `瞬間CPM: ${point.instantCpm}`,
    `平均CPM: ${point.avgCpm}`
  ];
  ctx.save();

  ctx.font = '12px "Noto Sans JP", sans-serif';
  const paddingX = 10;
  const paddingY = 8;
  const lineH = 18;
  const boxW = Math.max(...lines.map(line => ctx.measureText(line).width)) + paddingX * 2;
  const boxH = lines.length * lineH + paddingY * 2;

  let boxX = point.x + 12;
  let boxY = point.y - boxH - 12;
  if (boxX + boxW > cssW - 4) boxX = point.x - boxW - 12;
  if (boxY < 4) boxY = point.y + 12;
  if (boxY + boxH > cssH - 4) boxY = cssH - boxH - 4;

  ctx.fillStyle = getComputedStyle(document.body).getPropertyValue('--tooltip-bg').trim() || 'rgba(13, 15, 20, 0.92)';
  ctx.strokeStyle = colBorder;
  ctx.lineWidth = 1;
  roundRect(ctx, boxX, boxY, boxW, boxH, 8);
  ctx.fill();
  ctx.stroke();

  // 対象点を強調。
  ctx.fillStyle = colAccent2;
  ctx.beginPath();
  ctx.arc(point.x, point.y, 5, 0, Math.PI * 2);
  ctx.fill();

  ctx.fillStyle = getComputedStyle(document.body).getPropertyValue('--tooltip-text').trim() || '#e2e8f0';
  ctx.textAlign = 'left';
  ctx.textBaseline = 'top';
  lines.forEach((line, i) => {
    ctx.fillText(line, boxX + paddingX, boxY + paddingY + i * lineH);
  });
  ctx.restore();
}

function roundRect(ctx, x, y, w, h, r) {
  const radius = Math.min(r, w / 2, h / 2);
  ctx.beginPath();
  ctx.moveTo(x + radius, y);
  ctx.lineTo(x + w - radius, y);
  ctx.quadraticCurveTo(x + w, y, x + w, y + radius);
  ctx.lineTo(x + w, y + h - radius);
  ctx.quadraticCurveTo(x + w, y + h, x + w - radius, y + h);
  ctx.lineTo(x + radius, y + h);
  ctx.quadraticCurveTo(x, y + h, x, y + h - radius);
  ctx.lineTo(x, y + radius);
  ctx.quadraticCurveTo(x, y, x + radius, y);
}

function findNearestCPMPoint(clientX, clientY) {
  if (!cpmChart || !cpmChartPoints.length) return -1;
  const rect = cpmChart.getBoundingClientRect();
  const x = clientX - rect.left;
  const y = clientY - rect.top;
  let nearest = -1;
  let bestDist = Infinity;
  const hitRadius = 10;
  cpmChartPoints.forEach(point => {
    const dist = Math.hypot(point.x - x, point.y - y);
    if (dist <= hitRadius && dist < bestDist) {
      nearest = point.index;
      bestDist = dist;
    }
  });
  return nearest;
}

// 補助: 値を「キリのいい上限」に切り上げる（10/20/50/100/200/500...）
function niceCeil(v) {
  if (v <= 0) return 10;
  const pow = Math.pow(10, Math.floor(Math.log10(v)));
  const n = v / pow;
  let nice;
  if (n <= 1)      nice = 1;
  else if (n <= 2) nice = 2;
  else if (n <= 5) nice = 5;
  else             nice = 10;
  return nice * pow;
}

// 補助: 経過秒数の最大値に応じた目盛り間隔を返す
function chooseTimeStep(maxTime) {
  if (maxTime <= 10)  return 2;
  if (maxTime <= 30)  return 5;
  if (maxTime <= 60)  return 10;
  if (maxTime <= 180) return 30;
  if (maxTime <= 300) return 60;
  return 120;
}

// 補助: 秒を "mm:ss" もしくは "Ns" 表記に
function formatSec(sec) {
  if (sec < 60) return `${Math.round(sec)}s`;
  const m = Math.floor(sec / 60);
  const s = Math.round(sec % 60);
  return `${m}:${s.toString().padStart(2, '0')}`;
}

// 補助: CSS 変数経由で取得した色（hex想定）を rgba にする
function hexToRgba(hex, a) {
  const h = hex.replace('#', '');
  const full = h.length === 3 ? h.split('').map(c => c + c).join('') : h;
  const r = parseInt(full.substring(0, 2), 16);
  const g = parseInt(full.substring(2, 4), 16);
  const b = parseInt(full.substring(4, 6), 16);
  return `rgba(${r},${g},${b},${a})`;
}

// グラフ上の点にマウスを重ねたら、その時点の瞬間CPMと平均CPMを表示する。
if (cpmChart) {
  cpmChart.addEventListener('mousemove', (e) => {
    if (resultScreen.style.display !== 'block' || !cpmHistory.length) return;
    const nearest = findNearestCPMPoint(e.clientX, e.clientY);
    cpmChart.style.cursor = nearest >= 0 ? 'pointer' : 'crosshair';
    if (nearest !== cpmChartHoverIndex) {
      cpmChartHoverIndex = nearest;
      drawCPMChart(cpmChartHoverIndex);
    }
  });

  cpmChart.addEventListener('mouseleave', () => {
    if (cpmChartHoverIndex !== -1) {
      cpmChartHoverIndex = -1;
      cpmChart.style.cursor = 'crosshair';
      drawCPMChart();
    }
  });
}

// 結果画面が表示中にウィンドウサイズが変わったら、グラフを再描画する。
// 結果画面が非表示の間は何もしない（cssW=0 ガードでも防がれるが念のため）。
window.addEventListener('resize', () => {
  if (resultScreen.style.display === 'block' && cpmHistory.length > 0) {
    drawCPMChart(cpmChartHoverIndex);
  }
});

// =============================================================================
// 採点詳細（エラー分析）エンジン
// -----------------------------------------------------------------------------
// 入力文字列と正解文字列を比較して、以下のカテゴリに分類してカウント・一覧化する。
//   - 誤字           : 問題と異なる文字を打った
//   - 脱字           : 打ち落とした文字
//   - 余字           : 余分に挟み込まれた文字
//   - 余分な空白     : 正解にない場所に挿入された空白（全角・半角どちらも）
//   - 不要な改行     : 正解にない場所に挿入された改行
//   - 改行不足       : 必要な改行を打っていない
//   - 全角/半角違い  : 正解と「文字種は同じだが幅が違う」（例: A↔Ａ、1↔１）
//   - 句読点違い     : 正解と「読点同士の違い」「句点同士の違い」など
//                       例: 「、」↔「，」、「。」↔「．」、「,」↔「、」など
//
// 計算は LCS (Longest Common Subsequence) で位置合わせし、その編集スクリプトを
// もとに「置換 = 誤字寄り」「削除 = 脱字」「挿入 = 余字」と分類していく。
// PDF p.3 の速度採点基準（誤字＝1文字減、脱字／余字＝該当文字数分の減）を
// 簡易化した形で踏襲する。級判定は行わない（仕様による）。
// =============================================================================

// 全角→半角の対応表（数字・英字・記号の代表的なもの）。判定にのみ使う、置換はしない。
// マッピングは「左右どちらでも対応関係にある」と見るため、両方向にチェックする。
const FULLWIDTH_TO_HALF = (() => {
  const map = new Map();
  // 全角数字 -> 半角数字
  for (let i = 0; i < 10; i++) {
    map.set(String.fromCharCode(0xFF10 + i), String(i));
  }
  // 全角英字 -> 半角英字（大文字・小文字）
  for (let i = 0; i < 26; i++) {
    map.set(String.fromCharCode(0xFF21 + i), String.fromCharCode(0x41 + i));
    map.set(String.fromCharCode(0xFF41 + i), String.fromCharCode(0x61 + i));
  }
  // 全角記号 -> 半角記号（よく出るもの）
  const symPairs = [
    ['！','!'],['＃','#'],['＄','$'],['％','%'],['＆','&'],['（','('],['）',')'],
    ['＊','*'],['＋','+'],['－','-'],['．','.'],['／','/'],['：',':'],['；',';'],
    ['＜','<'],['＝','='],['＞','>'],['？','?'],['＠','@'],['［','['],['］',']'],
    ['｛','{'],['｝','}'],['＾','^'],['＿','_'],['｜','|'],['～','~'],['　',' ']
  ];
  symPairs.forEach(([f, h]) => map.set(f, h));
  return map;
})();

// 文字が空白（半角スペース、全角スペース、タブ）かどうか。改行は別カテゴリ扱い。
function isSpaceChar(ch) {
  return ch === ' ' || ch === '\u3000' || ch === '\t';
}

// 「文字種は同じだが全角/半角が違う」ペアかどうか。
// 例: 'A' と 'Ａ'、'1' と '１'、'　' と ' '。
function isWidthVariant(a, b) {
  if (a === b) return false;
  if (FULLWIDTH_TO_HALF.get(a) === b) return true;
  if (FULLWIDTH_TO_HALF.get(b) === a) return true;
  return false;
}

// 句読点違い（読点同士、句点同士の表記揺れ）かどうか。
// 同一文字は除外し、別の句読点に置き換わっているケースだけ true。
function isPunctuationVariant(a, b) {
  if (a === b) return false;
  const commas = new Set(['、','，',',']);
  const periods = new Set(['。','．','.']);
  if (commas.has(a) && commas.has(b)) return true;
  if (periods.has(a) && periods.has(b)) return true;
  return false;
}

// 文字を一覧表示用に整形する。空白・改行・タブは可視化用のラベルに変換。
function glyphify(ch) {
  if (ch === ' ')    return '<span class="glyph">SP</span>';
  if (ch === '\u3000') return '<span class="glyph">全SP</span>';
  if (ch === '\t')   return '<span class="glyph">TAB</span>';
  if (ch === '\n')   return '<span class="glyph">↵</span>';
  if (ch === '')     return '<span class="glyph">―</span>';
  return escapeHtml(ch);
}

// LCS を計算し、編集スクリプト（'eq' / 'sub' / 'del' / 'ins'）を返す。
// 入力: target=正解文字列, input=ユーザー入力。
// 戻り値: [{ op, ti, ii, tch, ich }] の配列。
//   op  = 'eq'（一致）/ 'sub'（置換）/ 'del'（脱字＝targetだけにある）/ 'ins'（余字＝inputだけにある）
//   ti  = target 側のインデックス、ii = input 側のインデックス
//   tch = target 側の文字、    ich = input 側の文字
// なお置換 (sub) は LCS そのものでは出ないが、del+ins が隣接した場合に
// 後段で 1 つの sub にマージしている（編集距離的な見方に近づける）。
function computeEditScript(target, input) {
  const n = target.length, m = input.length;
  // DP テーブルが巨大化するのを抑えるため、上限を設ける。
  // 想定: LONG_TEXT は約 1700 文字、入力もそれ前後。1700×1700 = ~290 万セル。
  // 各セル整数1個なので Int32Array で十分高速・省メモリ。
  const dp = new Int32Array((n + 1) * (m + 1));
  const W = m + 1;
  for (let i = 1; i <= n; i++) {
    for (let j = 1; j <= m; j++) {
      if (target[i - 1] === input[j - 1]) {
        dp[i * W + j] = dp[(i - 1) * W + (j - 1)] + 1;
      } else {
        const a = dp[(i - 1) * W + j];
        const b = dp[i * W + (j - 1)];
        dp[i * W + j] = a > b ? a : b;
      }
    }
  }
  // バックトレース
  const raw = [];
  let i = n, j = m;
  while (i > 0 && j > 0) {
    if (target[i - 1] === input[j - 1]) {
      raw.push({ op: 'eq', ti: i - 1, ii: j - 1, tch: target[i - 1], ich: input[j - 1] });
      i--; j--;
    } else if (dp[(i - 1) * W + j] >= dp[i * W + (j - 1)]) {
      raw.push({ op: 'del', ti: i - 1, ii: j, tch: target[i - 1], ich: '' });
      i--;
    } else {
      raw.push({ op: 'ins', ti: i, ii: j - 1, tch: '', ich: input[j - 1] });
      j--;
    }
  }
  while (i > 0) { raw.push({ op: 'del', ti: i - 1, ii: 0, tch: target[i - 1], ich: '' }); i--; }
  while (j > 0) { raw.push({ op: 'ins', ti: 0, ii: j - 1, tch: '', ich: input[j - 1] }); j--; }
  raw.reverse();

  // del と ins の連続ブロックを 1 つの「置換ブロック」として扱い、
  // 対応する位置ペアを sub にマージする。これにより
  //   target=ABC123, input=ＡＢＣ１２３
  // のように共通部分が一切ない場合でも、隣り合った del/ins 群を
  // 順番に sub にできる（=> 各文字を「全角半角の置換」として認識できる）。
  const out = [];
  let k = 0;
  while (k < raw.length) {
    const cur = raw[k];
    // 'eq' はそのまま流す
    if (cur.op === 'eq') { out.push(cur); k++; continue; }

    // この k から続く del/ins の連続ブロックを集める
    let blockEnd = k;
    while (blockEnd < raw.length && (raw[blockEnd].op === 'del' || raw[blockEnd].op === 'ins')) {
      blockEnd++;
    }
    const block = raw.slice(k, blockEnd);
    // ブロック内の del 群と ins 群をそれぞれ取り出す（元の順序を保つ）
    const dels = block.filter(x => x.op === 'del');
    const inss = block.filter(x => x.op === 'ins');
    // 対応する index でペアを作って sub に。残りはそれぞれ del/ins として残す。
    const pairLen = Math.min(dels.length, inss.length);
    for (let p = 0; p < pairLen; p++) {
      const d = dels[p], i = inss[p];
      out.push({ op: 'sub', ti: d.ti, ii: i.ii, tch: d.tch, ich: i.ich });
    }
    for (let p = pairLen; p < dels.length; p++) out.push(dels[p]);
    for (let p = pairLen; p < inss.length; p++) out.push(inss[p]);

    k = blockEnd;
  }
  return out;
}

// 編集スクリプトを元に、入力文字列と正解文字列の差分を「採点項目」に分類する。
// 戻り値: { counts: {...}, lists: {...} }
//   counts.misuse / missing / extra / space / newlineExtra / newlineMiss / width / punct
//   lists.misuse / missing / extra / spacing / widthPunct
function classifyErrors(target, input) {
  const script = computeEditScript(target, input);

  const counts = {
    misuse: 0, missing: 0, extra: 0,
    space: 0, newlineExtra: 0, newlineMiss: 0,
    width: 0, punct: 0,
  };
  const lists = {
    misuse: [],     // {pos, expected, actual}
    missing: [],    // {pos, expected}
    extra: [],      // {pos, actual}
    spacing: [],    // {pos, kind, expected, actual}   kind: 'extra-space' / 'extra-newline' / 'missing-newline'
    widthPunct: [], // {pos, kind, expected, actual}   kind: 'width' / 'punct'
  };

  for (const ev of script) {
    if (ev.op === 'eq') continue;

    if (ev.op === 'sub') {
      // 置換: 中身を見て「全角半角違い」「句読点違い」「ただの誤字」に振り分ける。
      if (isWidthVariant(ev.tch, ev.ich)) {
        counts.width++;
        lists.widthPunct.push({
          pos: ev.ti + 1, kind: 'width', expected: ev.tch, actual: ev.ich,
        });
      } else if (isPunctuationVariant(ev.tch, ev.ich)) {
        counts.punct++;
        lists.widthPunct.push({
          pos: ev.ti + 1, kind: 'punct', expected: ev.tch, actual: ev.ich,
        });
      } else {
        counts.misuse++;
        lists.misuse.push({
          pos: ev.ti + 1, expected: ev.tch, actual: ev.ich,
        });
      }
    } else if (ev.op === 'del') {
      // del: target にあるが input にない = 打ち落とし。
      // 中身が改行なら「改行不足」、それ以外は脱字。
      if (ev.tch === '\n') {
        counts.newlineMiss++;
        lists.spacing.push({
          pos: ev.ti + 1, kind: 'missing-newline', expected: '\n', actual: '',
        });
      } else {
        counts.missing++;
        lists.missing.push({ pos: ev.ti + 1, expected: ev.tch });
      }
    } else if (ev.op === 'ins') {
      // ins: input にあるが target にない = 余分。
      // 改行なら「不要な改行」、空白なら「余分な空白」、それ以外は余字。
      if (ev.ich === '\n') {
        counts.newlineExtra++;
        lists.spacing.push({
          pos: ev.ii + 1, kind: 'extra-newline', expected: '', actual: '\n',
        });
      } else if (isSpaceChar(ev.ich)) {
        counts.space++;
        lists.spacing.push({
          pos: ev.ii + 1, kind: 'extra-space', expected: '', actual: ev.ich,
        });
      } else {
        counts.extra++;
        lists.extra.push({ pos: ev.ii + 1, actual: ev.ich });
      }
    }
  }
  return { counts, lists };
}

// 採点結果を結果画面に流し込む。表示／非表示は applyDetailVisibility() が司る。
function renderDetailedScoring(input, classified, correctInScope) {
  const { counts, lists } = classified;

  // --- 上部サマリのカード（入力文字数・エラー総数・純字数） ---
  resInput.textContent = input.length;

  const errorTotal =
    counts.misuse + counts.missing + counts.extra +
    counts.space + counts.newlineExtra + counts.newlineMiss +
    counts.width + counts.punct;
  resErrorTotal.textContent = errorTotal;

  // 純字数 = 入力文字数 - （エラー総数 × 10）。
  // 失格ラインに達した場合は純字数を 0 にし、判定を「失格」にする。
  // エラー 0 かつ純字数 1000 以上の場合は「正確賞」と表示する。
  const deduction = errorTotal * 10;
  const disqualifyLimit = disqualifyLimitSelect ? parseInt(disqualifyLimitSelect.value, 10) : 10;
  const isDisqualified = errorTotal >= disqualifyLimit;
  const rawNet = Math.max(0, input.length - deduction);
  const net = isDisqualified ? 0 : rawNet;

  resNet.textContent = net;
  if (resDeduction) resDeduction.textContent = deduction;

  if (resJudge) {
    resJudge.classList.remove('judge-ok', 'judge-prize', 'judge-disqualified');
    if (isDisqualified) {
      resJudge.textContent = '失格';
      resJudge.classList.add('judge-disqualified');
      if (resJudgeNote) resJudgeNote.textContent = `エラー数が${disqualifyLimit}件以上`;
    } else if (errorTotal === 0 && net >= 1000) {
      resJudge.textContent = '正確賞';
      resJudge.classList.add('judge-prize');
      if (resJudgeNote) resJudgeNote.textContent = 'エラー0・純字数1000以上';
    } else {
      resJudge.textContent = '通常';
      resJudge.classList.add('judge-ok');
      if (resJudgeNote) resJudgeNote.textContent = `失格ライン ${disqualifyLimit}件`;
    }
  }

  // --- エラー内訳カード ---
  errMisuseCount.textContent       = counts.misuse;
  errMissingCount.textContent      = counts.missing;
  errExtraCount.textContent        = counts.extra;
  errSpaceCount.textContent        = counts.space;
  errNewlineExtraCount.textContent = counts.newlineExtra;
  errNewlineMissCount.textContent  = counts.newlineMiss;
  errWidthCount.textContent        = counts.width;
  errPunctCount.textContent        = counts.punct;

  // --- 各一覧 ---
  // 一覧は最大件数までに制限（巨大な誤入力時のレンダリング負荷対策）。
  const MAX_LIST = 200;
  fillList(listMisuse, lists.misuse.slice(0, MAX_LIST), (e) =>
    `<span class="el-pos">#${e.pos}</span>
     <span class="el-expected"><span class="lbl">正</span>${glyphify(e.expected)}</span>
     <span class="el-actual"><span class="lbl">入</span>${glyphify(e.actual)}</span>`);

  fillList(listMissing, lists.missing.slice(0, MAX_LIST), (e) =>
    `<span class="el-pos">#${e.pos}</span>
     <span class="el-expected"><span class="lbl">正</span>${glyphify(e.expected)}</span>
     <span class="el-actual"><span class="lbl">入</span>${glyphify('')}</span>`);

  fillList(listExtra, lists.extra.slice(0, MAX_LIST), (e) =>
    `<span class="el-pos">#${e.pos}</span>
     <span class="el-expected"><span class="lbl">正</span>${glyphify('')}</span>
     <span class="el-actual"><span class="lbl">入</span>${glyphify(e.actual)}</span>`);

  fillList(listSpacing, lists.spacing.slice(0, MAX_LIST), (e) => {
    const kindLabel =
      e.kind === 'extra-space'    ? '余分な空白' :
      e.kind === 'extra-newline'  ? '不要な改行' :
      e.kind === 'missing-newline'? '改行不足' : e.kind;
    return `<span class="el-pos">#${e.pos} ${kindLabel}</span>
            <span class="el-expected"><span class="lbl">正</span>${glyphify(e.expected)}</span>
            <span class="el-actual"><span class="lbl">入</span>${glyphify(e.actual)}</span>`;
  });

  fillList(listWidthPunct, lists.widthPunct.slice(0, MAX_LIST), (e) => {
    const kindLabel = e.kind === 'width' ? '全角/半角' : '句読点';
    return `<span class="el-pos">#${e.pos} ${kindLabel}</span>
            <span class="el-expected"><span class="lbl">正</span>${glyphify(e.expected)}</span>
            <span class="el-actual"><span class="lbl">入</span>${glyphify(e.actual)}</span>`;
  });
}

// ul に <li> を埋める。空のときは「該当なし」を一行だけ表示。
function fillList(ulEl, items, formatter) {
  if (!ulEl) return;
  if (items.length === 0) {
    ulEl.innerHTML = '<li class="ds-empty" style="display:block;border:none;">該当なし</li>';
    return;
  }
  ulEl.innerHTML = items.map(item => `<li data-pos="${item.pos}">${formatter(item)}</li>`).join('');
  ulEl.querySelectorAll('li[data-pos]').forEach(li => {
    li.addEventListener('click', () => focusTextPosition(parseInt(li.dataset.pos, 10)));
  });
}

// 結果画面のエラー一覧をクリックしたとき、課題文表示欄の該当位置へ移動して強調する。
function focusTextPosition(pos) {
  if (!Number.isFinite(pos) || pos <= 0) return;
  const target = textDisplay.querySelector(`[data-index="${pos}"]`) || textDisplay.querySelector('.char-cursor');
  if (!target) return;
  textDisplay.querySelectorAll('.char-review-focus').forEach(el => el.classList.remove('char-review-focus'));
  target.classList.add('char-review-focus');
  target.scrollIntoView({ behavior: 'smooth', block: 'center', inline: 'nearest' });
}

// 採点詳細の計算 → 描画 → 表示制御 を一括で行うエントリポイント。
// endGame の最後から呼ばれる。
function runDetailedScoring(input) {
  // ★ 採点範囲は「入力が終了した部分まで」とする。
  //    課題文全体（LONG_TEXT）と比較すると、未入力部分が全部「脱字」になってしまうため、
  //    入力末尾までに対応する正解側の長さを求めて、その範囲だけを採点対象にする。
  //
  //  実装方針:
  //    1) まず LONG_TEXT 全体と input で LCS を計算し、編集スクリプトを取る。
  //    2) スクリプトを末尾から走査して、「入力側 (ii) がもう登場しなくなった位置」を探す。
  //       そこから先の del（target だけ進む）は「まだ打っていない部分」なので採点から除外する。
  //    3) その境界の target インデックスまでを effectiveTarget とし、これに対して
  //       改めて分類を行う（スクリプトをトリムしてもよいが、再計算のほうがシンプル）。
  //
  //  例: target="あいうえお", input="あxいうえ" の場合
  //       入力が消化した target は "あいうえ" まで（5文字目「お」は未入力）。
  //       これにより「お」を脱字として誤検出しない。
  const effectiveTarget = computeEffectiveTarget(LONG_TEXT, input);
  const classified = classifyErrors(effectiveTarget, input);
  const correctInScope = countCorrectInScope(effectiveTarget, input);
  renderDetailedScoring(input, classified, correctInScope);
  applyDetailVisibility();
}

// 採点対象範囲内で一致している文字数を数える。
// 詳細採点の「純字数」用。未入力の残り部分はここにも含めない。
function countCorrectInScope(target, input) {
  let correct = 0;
  const len = Math.min(target.length, input.length);
  for (let i = 0; i < len; i++) {
    if (target[i] === input[i]) correct++;
  }
  return correct;
}

// 入力が「課題文のどこまで進んだか」を判定し、その範囲までの正解文字列を返す。
// LCS の編集スクリプトを末尾から見て、最後に入力側 (ii) が使われた位置 +1 を
// 「入力が到達した target 側の境界」とみなす。
function computeEffectiveTarget(target, input) {
  // 採点対象は「入力済みの文字数ぶん」だけに限定する。
  // ここで target 全文や LCS 由来の到達位置を使うと、まだ入力していない後続部分が
  // trailing del として残り、「脱字」「改行不足」に誤カウントされることがある。
  // そのため、入力文字数と同じ長さの課題文先頭部分だけを採点対象にする。
  //
  // 例:
  //   target = "あいう\nえお"
  //   input  = "あいう"
  //   effectiveTarget = "あいう"
  //   => まだ打っていない改行は「改行不足」にしない。
  //
  // 入力が課題文全文より長い場合は、課題文全文までを対象にし、超過分は classifyErrors 側で
  // 「余字」として扱う。
  const scopeLen = Math.min(input.length, target.length);
  return target.substring(0, scopeLen);
}

function applyDisplayMode() {
  // 結果画面が独立したため、結果は常に詳細表示を基本とする。
  // 個別の採点詳細は結果画面内のチェックボックスで調整する。
  document.body.classList.remove('simple-mode');
  applyDetailVisibility();
  if (resultScreen.style.display === 'block') {
    requestAnimationFrame(() => drawCPMChart(cpmChartHoverIndex));
  }
}

function applyThemeMode() {
  const mode = themeModeSelect ? themeModeSelect.value : 'light';
  document.body.classList.toggle('theme-dark', mode === 'dark');
  document.body.classList.toggle('theme-light', mode !== 'dark');
  if (resultScreen.style.display === 'block') {
    requestAnimationFrame(() => drawCPMChart(cpmChartHoverIndex));
  }
}

function applyAccessibilityMode() {
  const mode = accessibilityModeSelect ? accessibilityModeSelect.value : 'colorblind';
  document.body.classList.toggle('colorblind-mode', mode === 'colorblind');
  if (resultScreen.style.display === 'block') {
    requestAnimationFrame(() => drawCPMChart(cpmChartHoverIndex));
  }
}


function applyTypingPositionMode() {
  const mode = typingPositionModeSelect ? typingPositionModeSelect.value : 'hide';
  document.body.classList.toggle('show-typing-position', mode === 'show');
  const currentInput = typingArea ? typingArea.value : '';
  if (resultScreen.style.display !== 'block') {
    renderTextDisplay(currentInput);
  }
}

function applyLiveStatusMode() {
  const mode = liveStatusModeSelect ? liveStatusModeSelect.value : 'show';
  document.body.classList.toggle('hide-live-status', mode === 'hide');
}

// チェックボックスの状態に応じて、対応する [data-detail-key] 要素を表示／非表示する。
// 表示するときは block / grid / list-item を style に頼らず、空文字に戻すだけでよい。
function applyDetailVisibility() {
  const toggles = document.querySelectorAll('#scoring-settings input[type="checkbox"][data-toggle]');
  toggles.forEach(cb => {
    const key = cb.dataset.toggle;
    // 同じキーを持つ要素が複数あってもよい（カード＆セクションをまとめて切り替える）。
    const targets = document.querySelectorAll(`[data-detail-key="${key}"]`);
    targets.forEach(el => {
      el.style.display = cb.checked ? '' : 'none';
    });
  });
}

// チェックボックスの変更をリッスン。結果画面表示前にも貼っておけば、
// 後から表示されてもチェック状態が反映される。
document.querySelectorAll('#scoring-settings input[type="checkbox"][data-toggle]').forEach(cb => {
  cb.addEventListener('change', applyDetailVisibility);
});

if (feedbackModeSelect) {
  feedbackModeSelect.addEventListener('change', () => {
    if (!running && resultScreen.style.display !== 'block') renderTextDisplay('');
  });
}


if (liveStatusModeSelect) {
  liveStatusModeSelect.addEventListener('change', applyLiveStatusMode);
}

if (themeModeSelect) {
  themeModeSelect.addEventListener('change', applyThemeMode);
}

if (accessibilityModeSelect) {
  accessibilityModeSelect.addEventListener('change', applyAccessibilityMode);
}

if (typingPositionModeSelect) {
  typingPositionModeSelect.addEventListener('change', applyTypingPositionMode);
}

if (btnConfigToggle) {
  btnConfigToggle.addEventListener('click', () => {
    const isOpen = advancedSettings && !advancedSettings.classList.contains('is-collapsed');
    setAdvancedSettingsOpen(!isOpen);
  });
}

setAdvancedSettingsOpen(false);
loadTexts();
applyDisplayMode();
applyLiveStatusMode();
applyThemeMode();
applyAccessibilityMode();
applyTypingPositionMode();

if (btnTextLibrary) {
  btnTextLibrary.addEventListener('click', openTextLibrary);
}
if (btnCloseLibrary) {
  btnCloseLibrary.addEventListener('click', closeTextLibrary);
}
if (textLibraryModal) {
  textLibraryModal.addEventListener('click', (e) => {
    if (e.target === textLibraryModal) closeTextLibrary();
  });
}
