// DOM参照
// HTML側のID変更時は、まずこのファイルを確認する。

const timeSelect      = document.getElementById('time-select');
const taskTitle       = document.getElementById('task-title');
const startModeSelect = document.getElementById('start-mode');
const displayPresetModeSelect = document.getElementById('display-preset-mode');
const manualDetailModeCheckbox = document.getElementById('manual-detail-mode');
const feedbackModeSelect = document.getElementById('feedback-mode');
const correctFeedbackModeSelect = document.getElementById('correct-feedback-mode');
// 結果画面は独立画面で常時表示するため、ホーム画面の結果表示選択は廃止。
const liveStatusModeSelect = document.getElementById('live-status-mode');
const themeModeSelect = document.getElementById('theme-mode');
const focusDisplayModeCheckbox = document.getElementById('focus-display-mode');
const accessibilityModeSelect = document.getElementById('accessibility-mode');
const typingPositionModeSelect = document.getElementById('typing-position-mode');
const timeCallModeSelect = document.getElementById('time-call-mode');
const btnConfigToggle = document.getElementById('btn-config-toggle');
const advancedSettings = document.getElementById('advanced-settings');
const disqualifyLimitSelect = document.getElementById('disqualify-limit');
const btnStart        = document.getElementById('btn-start');
const btnAbort        = document.getElementById('btn-abort');
const btnRetry        = document.getElementById('btn-retry');
const btnRetryRandom  = document.getElementById('btn-retry-random');
const btnBackConfig   = document.getElementById('btn-back-config');
const btnOpenRecordsHome = document.getElementById('btn-open-records-home');
const btnOpenRecordsResult = document.getElementById('btn-open-records-result');
const btnRecordBack = document.getElementById('btn-record-back');
const btnRecordHome = document.getElementById('btn-record-home');
const timerDisplay    = document.getElementById('timer-display');
const timerLabel      = document.getElementById('timer-label');
const timerPill       = document.getElementById('timer-pill');
const correctDisplay  = document.getElementById('correct-display');
const missDisplay     = document.getElementById('miss-display');
const cpmDisplay      = document.getElementById('cpm-display');
const progressDisplay = document.getElementById('progress-display');
const progressBar     = document.getElementById('progress-bar');
const textDisplay     = document.getElementById('text-display');
const typingArea      = document.getElementById('typing-area');
const resultScreen    = document.getElementById('result-screen');
const recordsScreen   = document.getElementById('records-screen');
const resCorrect      = document.getElementById('res-correct');
const resBackspace    = document.getElementById('res-backspace');
const resAccuracy     = document.getElementById('res-accuracy');
const resCpm          = document.getElementById('res-cpm');
const resCps          = document.getElementById('res-cps');
const resElapsed      = document.getElementById('res-elapsed');
const resTaskTitle    = document.getElementById('res-task-title');
const resCondition    = document.getElementById('res-condition');
const resultSummaryText = document.getElementById('result-summary-text');
const countdownOverlay = document.getElementById('countdown-overlay');
const timeCall = document.getElementById('time-call');
const cpmChart        = document.getElementById('cpm-chart');
const btnCpmPlay      = document.getElementById('btn-cpm-play');
const btnCpmReset     = document.getElementById('btn-cpm-reset');
const chartCurrentTime = document.getElementById('chart-current-time');
const chartCurrentAvgCpm = document.getElementById('chart-current-avg-cpm');
const chartCurrentInstantCpm = document.getElementById('chart-current-instant-cpm');

const recordCurrentNote = document.getElementById('record-current-note');
const recordHistoryBody = document.getElementById('record-history-body');
const recordFilterMode = document.getElementById('record-filter-mode');
const recordSortMode = document.getElementById('record-sort-mode');
const recordSummaryCount = document.getElementById('record-summary-count');
const recordSummaryCpm = document.getElementById('record-summary-cpm');
const recordSummaryAccuracy = document.getElementById('record-summary-accuracy');
const recordSummaryError = document.getElementById('record-summary-error');
const recordHistoryTitle = document.getElementById('record-history-title');

// --- 採点詳細関連の DOM 参照 -----------------------------------------------
// 結果画面に追加した「採点詳細」「表示設定」関連の要素をまとめて取得しておく。
// 既存ロジックには触れず、これらは endGame の最後と表示設定変更時にだけ使う。
const resInput            = document.getElementById('res-input');
const resErrorTotal       = document.getElementById('res-error-total');
const resNet              = document.getElementById('res-net');
const resDeduction        = document.getElementById('res-deduction');
const resJudge            = document.getElementById('res-judge');
const resJudgeNote        = document.getElementById('res-judge-note');
const errMisuseCount      = document.getElementById('err-misuse-count');
const errMissingCount     = document.getElementById('err-missing-count');
const errExtraCount       = document.getElementById('err-extra-count');
const errSpaceCount       = document.getElementById('err-space-count');
const errNewlineExtraCount= document.getElementById('err-newline-extra-count');
const errNewlineMissCount = document.getElementById('err-newline-miss-count');
const errWidthCount       = document.getElementById('err-width-count');
const errPunctCount       = document.getElementById('err-punct-count');
const missPunctCount      = document.getElementById('miss-punct-count');
const missDigitCount      = document.getElementById('miss-digit-count');
const missAlphaCount      = document.getElementById('miss-alpha-count');
const missSymbolCount     = document.getElementById('miss-symbol-count');
const missNewlineCount    = document.getElementById('miss-newline-count');
const missOtherCount      = document.getElementById('miss-other-count');
const missPunctRate       = document.getElementById('miss-punct-rate');
const missDigitRate       = document.getElementById('miss-digit-rate');
const missAlphaRate       = document.getElementById('miss-alpha-rate');
const missSymbolRate      = document.getElementById('miss-symbol-rate');
const missNewlineRate     = document.getElementById('miss-newline-rate');
const missOtherRate       = document.getElementById('miss-other-rate');
const missAnalysisSummary = document.getElementById('miss-analysis-summary');
const listMissAnalysis    = document.getElementById('list-miss-analysis');
const listMisuse          = document.getElementById('list-misuse');
const listMissing         = document.getElementById('list-missing');
const listExtra           = document.getElementById('list-extra');
const listSpacing         = document.getElementById('list-spacing');
const listWidthPunct      = document.getElementById('list-width-punct');
const sectionAnalysis      = document.getElementById('section-analysis');
const sectionEarlyCpm      = document.getElementById('section-early-cpm');
const sectionEarlyDetail   = document.getElementById('section-early-detail');
const sectionMiddleCpm     = document.getElementById('section-middle-cpm');
const sectionMiddleDetail  = document.getElementById('section-middle-detail');
const sectionLateCpm       = document.getElementById('section-late-cpm');
const sectionLateDetail    = document.getElementById('section-late-detail');
const sectionAnalysisSummary = document.getElementById('section-analysis-summary');
const recentComparison      = document.getElementById('recent-comparison');
const recentCompareCpm      = document.getElementById('recent-compare-cpm');
const recentCompareCpmDetail = document.getElementById('recent-compare-cpm-detail');
const recentCompareAccuracy = document.getElementById('recent-compare-accuracy');
const recentCompareAccuracyDetail = document.getElementById('recent-compare-accuracy-detail');
const recentCompareError    = document.getElementById('recent-compare-error');
const recentCompareErrorDetail = document.getElementById('recent-compare-error-detail');
const recentComparisonSummary = document.getElementById('recent-comparison-summary');


